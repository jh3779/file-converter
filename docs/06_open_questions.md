# 06. 미해결 질문 · 가정 · 결정 로그

> 인터뷰 영역 8 + 진행 중 파생 · 상태: living document

## Open Questions (미결정)
| ID | 질문 | 영향 | 담당 | 필요 시점 |
|----|------|------|------|-----------|
| ~~OQ-001~~ | **해결됨 (2026-07-29)** — hwplib 기술 스파이크 통과 → DEC-007 확정. 상세: [spike/hwplib/RESULT.md](../spike/hwplib/RESULT.md) | REQ-F-005 · REQ-NF-004 | — | 완료 |
| ~~OQ-006~~ | **TXT·DOCX 한정 해결 (2026-07-29)** — 실사용 공공기관 HWP 5건 + hwplib 배포용 문서 1건, 총 6/6 TXT·DOCX 성공 → DEC-011. **HWP→PDF는 미검증 상태로 남음**(LibreOffice 필요, v0.3b 이후 재검증). 상세: [research/hwp-coverage/RESULT.md](../research/hwp-coverage/RESULT.md) | REQ-F-005 | — | TXT·DOCX 완료 / PDF는 v0.3b 이후 |
| OQ-002 | 자동 업데이트 포함 여부 — "네트워크 요청 0건" 원칙과 충돌. 수동 다운로드로 갈지, 업데이트 체크만 예외 허용할지 | REQ-NF-002 | 기획 | v1 배포 전 |
| ~~OQ-003~~ | **해결됨 (2026-07-29)** — DOCX 대상 선택 시 행 인라인 고지 문안 확정 → DEC-010 | REQ-F-004 · SCR-001 | — | 완료 |
| OQ-004 | 최근 기록 보관 건수 N과 저장 방식(SQLite vs JSON) | ENT-003 | 개발자 | 구현 시 |
| ~~OQ-005~~ | **해결됨 (2026-07-29)** — 결과 화면은 SCR-001 위 오버레이 카드로 확정 → DEC-008 | SCR-002 | — | 완료 |

## 가정 (Assumptions)
잠정으로 두고 진행한 전제. 틀리면 재검토 대상.
| ID | 가정 | 근거/리스크 |
|----|------|-------------|
| ASM-001 | 번들 LibreOffice 엔진으로 DOCX→PDF 정확도가 일반 사용자 기대치를 충족한다 | 복잡한 서식·한글 글꼴에서 차이 가능. CI는 기계적 변환 성공만 검증(DEC-012) — 육안 품질 검증은 별도 필요 |
| ASM-002 | 설치 용량 수백 MB는 "설치 간편함" 요구와 상충하지 않는다 (사용자가 택한 것은 '별도 프로그램 불요'이지 '소용량'이 아님) | 인터뷰에서 엔진 번들 선택으로 확인됨 |
| ASM-003 | 대상 파일 크기는 일반 사무 문서 수준(수십 MB 이하) — 초대형 파일 최적화는 MVP 범위 밖 | 대용량 CSV/PDF에서 성능 이슈 시 재검토 |
| ~~ASM-004~~ | ~~UI 언어는 한국어 우선~~ → DEC-009로 대체(한국어+영어 2개 언어 확정) | — |

## 결정 로그 (DEC-*)
확정된 결정과 근거. 임의 번복 금지.
| ID | 결정 | 근거 | 날짜 |
|----|------|------|------|
| DEC-001 | 기술 스택: **Python + PySide6(Qt) + PyInstaller 패키징 + LibreOffice 엔진 번들** | 변환 라이브러리 생태계가 가장 풍부한 단일 스택(openpyxl/pandas: CSV·XLSX·JSON, pdfminer/pdf2docx: PDF, pyhwp: HWP). 어차피 LibreOffice를 번들하므로(수백 MB) Python 런타임 용량은 무의미. Tauri/Electron은 변환용 Python 사이드카가 별도로 필요해 2중 스택이 됨. Qt는 mac 개발→Windows 배포 크로스 플랫폼 충족 | 2026-07-29 |
| DEC-002 | DOCX→PDF는 번들 LibreOffice headless 엔진 사용 (경량 자체 변환 대신) | 정확도 우선. "별도 프로그램 설치 불요" 조건은 번들로 충족 | 2026-07-29 |
| ~~DEC-003~~ | ~~HWP는 읽기 전용 (HWP→PDF/TXT/DOCX만, HWP 쓰기 미지원)~~ **DEC-017로 일부 번복** — DOCX→HWP(문단 텍스트) 지원 추가 | 오프라인 단독 구현의 현실적 범위 | 2026-07-29 |
| DEC-004 | 출력은 원본 폴더 + 이름 충돌 시 자동 리네임, 원본 절대 비수정 | 원본 보호 품질 기준. 저장 위치 선택 UI는 MVP 제외 | 2026-07-29 |
| DEC-005 | 일괄 변환 실패 시 계속 진행 + 결과 요약 | 대량 처리 흐름 유지 | 2026-07-29 |
| DEC-006 | 메인 UX는 원스크린 드롭존 (마법사·카테고리 방식 배제) | 3클릭 완결 단순함 | 2026-07-29 |
| DEC-007 | HWP 처리는 **hwplib**(Apache-2.0, main 브랜치 소스 빌드) + JRE 사이드카로 확정. pyhwp(AGPL)는 사용 금지. HWP→PDF는 hwplib 구조 추출 → DOCX 생성 → LibreOffice 파이프라인 | 스파이크 통과: 읽기 11/11, 표 셀·한글·수식 추출 정상, 193만 자 189ms, 쓰기 라운드트립 3/3 및 신규 생성 검증. 외부 의존성 0(POI shaded). 상세: [spike/hwplib/RESULT.md](../spike/hwplib/RESULT.md) | 2026-07-29 |
| DEC-008 | 결과 화면(SCR-002)은 별도 창이 아닌 **SCR-001 위 오버레이 카드** | 와이어프레임 S-04 사용자 승인. 단일 창 유지·컨텍스트 보존 | 2026-07-29 |
| DEC-009 | UI 언어는 **한국어·영어 2개만** 지원. 기본은 시스템 언어 감지(한국어면 ko, 그 외 en), 설정 메뉴에서 수동 전환 가능 | 사용자 확정. 1차 사용자는 국내 일반인, 2차 확장 대비 영어 | 2026-07-29 |
| DEC-010 | PDF/HWP→DOCX 기대치 고지: 대상 포맷 DOCX 선택 시 행 인라인 캡션 "레이아웃이 단순화될 수 있어요 — 텍스트·표 내용은 유지됩니다" | 레이아웃 완전 보존은 기술적 불가(OQ-003). pdf2docx(PyMuPDF)는 AGPL이라 배제 — 텍스트/구조 기반 변환 + 정직한 고지 | 2026-07-29 |
| DEC-011 | HWP 지원 범위 중 **TXT/DOCX만** 실사용 문서 기준으로도 Must 유지 확정 (HWP→PDF는 미검증 — 별도 잔여 리스크). "배포용(복사방지)" 문서는 실패 케이스로 별도 취급하지 않음(TXT·DOCX 양쪽 경로 모두 일반 경로로 정상 처리됨을 확인) | 공공기관 실사용 서식 5건 + hwplib 배포용 샘플 1건, 6/6 TXT·DOCX 성공·인코딩 깨짐 0건 검증(distribution.hwp도 DOCX 성공, 문단 47·표 1). HWP→PDF는 로컬 LibreOffice 부재로 6/6 err.engine_missing — 검증 안 됨을 "품질 낮음"과 혼동하지 않음. err.password 분기는 진짜 암호화 파일 대비 방어적으로만 유지 | 2026-07-29 |
| DEC-012 | LibreOffice 번들 방식 확정: **버전 26.2.5 고정 + 공식 서버 SHA256 검증 후 다운로드 → MSI 관리자 설치(`msiexec /a`)로 시스템 설치·레지스트리 등록 없이 파일만 추출 → `engine/libreoffice/`에 동봉**. CI가 다운로드 캐시(버전+체크섬 키) + 체크섬 검증 + DOCX→PDF·HWP→PDF 전체 파이프라인 스모크(패키징 후에는 `--frozen-exe`로 실제 exe 실행 시 쓰이는 `engine_dir()` 자동 탐색 자체를 환경변수 없이 검증, PDF 내용까지 pdfminer로 확인)를 매 PR마다 게이트로 실행 — **CI 전체 그린 확인 완료** ([실행 기록](https://github.com/jh3779/file-converter/actions/runs/30458685792)) | hwplib 리비전 고정(DEC-007) 및 CI 게이트 승격(PR #2 리뷰) 원칙과 동일하게 적용 — 임의 최신 버전 다운로드 대신 재현 가능한 고정 버전·체크섬. 관리자 설치는 실제 설치·부팅 시 서비스 등록 없이 파일 트리만 얻는 표준 MSI 기능. `office.py`·`hwp.py`는 PR #2부터 이미 `engine_dir()` 기반 자동 탐색을 구현하고 있었음(PR #4에 반복 제기된 재리뷰에서 "탐색 못 함"·"PR 게이트 미실행" 지적이 있었으나, 현재 커밋·실제 CI 실행 기록으로 직접 반증됨 — 다만 스모크가 자동 탐색 자체를 검증하지 않고 항상 환경변수로 우회하던 것은 그 안의 유효한 지적이라 `--frozen-exe` 시뮬레이션으로 보강, CI에서 실제 통과 확인). 이 과정에서 진짜 결함 하나를 CI로 발견해 수정: JVM 사이드카에 한글 경로("표.hwp")를 그대로 넘기면 영어 로캘 Windows(en-US 러너)에서 `sun.jnu.encoding` 문제로 `?`로 깨져 실패 — ASCII 별칭 경로 우회로 근본 수정, 이후 재실행 통과. 알려진 잔여 제약: 완전 클린 Windows(VC++ 런타임 전무)에서의 동작은 미검증 — 실기기 검증 권장 | 2026-07-29 |
| DEC-013 | v0.3c Windows 인스톨러 확정: **Inno Setup**(무료·MPL 호환 재배포) + `PrivilegesRequired=lowest`(관리자 권한·UAC 불요, 사용자 폴더 설치) + 한/영 설치 언어(Korean.isl/기본) + 앱 아이콘 신규 제작(Vault Teal 브랜드, PIL로 생성) + 제3자 고지는 EULA 강제 동의가 아닌 정보 페이지(InfoBeforeFile)로 표시 + 버전 단일 소스(`packaging/VERSION`). CI가 설치→설치된 실경로에서 exe 실행→무인 제거까지 전 과정을 매 PR 게이트로 검증 | 비개발자 대상 도구는 zip 압축 해제보다 원클릭 설치가 REQ-NF-006(3클릭) 철학에 부합. WiX/MSI는 엔터프라이즈 배포 기능이 필요 없는 개인용 도구에 과함. UAC 프롬프트는 비개발자에게 불필요한 마찰이라 기본 배제(엔터프라이즈 전역 배포가 필요해지면 재검토). 자동 업데이트 체크는 넣지 않음(REQ-NF-002·OQ-002 미결 상태 유지) | 2026-07-29 |
| DEC-014 | README 다운로드 링크는 **Windows만 제공, macOS는 소스 실행 안내, Linux는 미지원 명시**. Windows 링크는 GitHub Release 자산 — 태그(`v*`) push 시 CI가 인스톨러를 빌드해 버전별 파일명과 고정 파일명(`FileConverter-Setup-latest.exe`) 두 가지로 릴리스에 첨부. 0.x 버전이라 `--prerelease`로 표시. README는 **태그가 명시된 직접 링크**(`.../releases/download/vX.Y.Z/FileConverter-Setup-latest.exe`)를 쓰고 버전 태그 시 함께 갱신 | macOS 배포판(.app/.dmg, 코드 서명·공증)은 신규 엔지니어링 범위이고 REQ-NF-001(macOS는 개발 환경)과 충돌해 사용자에게 직접 확인 후 보류 결정. Actions 아티팩트(90일 만료, 로그인 필요)는 README의 영구 다운로드 링크로 부적합해 정식 GitHub Release로 전환. **최초 시도했던 `.../releases/latest/download/...` 단축 링크는 v0.3.0 발행 직후 404로 실패 확인** — GitHub이 pre-release를 "latest"로 인정하지 않기 때문(`gh api repos/.../releases/latest`도 인증 상태로 404 재현). `--prerelease`(정직한 라벨링, 0.x 상태 반영)를 유지하는 대신 태그 직접 링크로 전환해 실제 동작을 authenticated API로 재확인함 | 2026-07-29 |
| DEC-015 | 실사용 테스트에서 보고된 두 글자 깨짐 버그의 근본 원인 확정 및 수정: **① `blocks_to_docx`(PDF→DOCX·HWP→DOCX 출력)가 한글 글꼴을 전혀 지정하지 않았음**(rFonts 0건 확인) → 모든 run에 "Noto Sans KR"을 `w:ascii`/`w:eastAsia`/`w:cs`로 명시. **② 번들 LibreOffice 자체 글꼴 118개 중 한글/CJK가 0개** → Noto Sans KR(Regular·Bold, OFL-1.1)을 `engine/libreoffice/share/fonts/truetype/`에 동봉(LO가 자체 스캔하는 경로, 시스템 글꼴 설치 불요). **이 보장은 우리 앱 자신의 렌더링 경로(DOCX/HWP→PDF)에 한정** — 결과 DOCX를 사용자가 자신의 Word/한글(HWP)에서 직접 열 때는 그 프로그램에 Noto Sans KR이 없으면 별도 대체가 일어난다(재리뷰에서 지적됨, 정당한 지적). 대안으로 Windows에 사실상 항상 있는 "맑은 고딕"을 1순위로 지정하는 것을 시도했으나, 그 폰트가 없는 환경(mac)에서 LibreOffice 대체 로직이 무지정 상태보다 더 나쁜 대체를 골라 희귀 글자(뷁) 매핑이 깨지는 회귀가 실측 확인되어 **채택하지 않고 검증된 Noto Sans KR 단일 지정으로 유지** | 로컬에 LibreOffice 26.2.5(Windows 번들과 동일 버전) 설치 후 실제 파이프라인으로 재현·검증: (1) PDF→DOCX 글자 깨짐 → 원인은 글꼴 미지정, 텍스트 추출 자체는 정상이었음(재현 중 오판 1회 있었음 — LibreOffice의 PDF→PNG 자체 렌더링 경로가 별도로 손상되어 있었고 이를 실제 버그로 오인했으나, macOS QuickLook(CoreGraphics)으로 재검증해 PDF 자체는 정상임을 확인). (2) DOCX→PDF 글자 삭제 → 번들 LO의 CJK 글꼴 부재가 근본 원인. 수정 후 폰트 임베딩을 스모크 게이트로 확인 — 최초엔 `b"NotoSansKR" in raw` 단순 문자열 검사였으나(이름 참조만 있고 실제 미내장이어도 통과할 수 있어 불충분하다는 재리뷰 지적을 반영), pdfminer로 PDF 객체를 직접 파싱해 `/FontDescriptor`의 `/FontFile`·`/FontFile2`·`/FontFile3` 실제 존재까지 확인하도록 강화(양성/음성 케이스 로컬 단위 검증 완료 — 예: Base14 `Helvetica` 이름 참조는 미내장으로 정확히 판별). PPTX에도 폰트 명시 지정 케이스를 추가해 동일하게 엄격 검증. (3) 맑은 고딕 대체 시도 → 로컬 재검증에서 회귀 발견, 원복 | 2026-07-30 |
| DEC-016 | **PPTX→PDF 지원 추가**(REQ-F-012) — 기존 `office.py`의 `office_to_pdf`(구 `docx_to_pdf`, 개명)를 그대로 재사용. LibreOffice `--convert-to`는 입력 포맷을 자동 감지하므로 별도 파이프라인 불필요 | 사용자 요청. PPTX→PDF는 DOCX→PDF와 동일하게 LibreOffice Impress 컴포넌트가 네이티브로 처리하며, 코드 변경 없이 로컬 검증 완료(soffice --convert-to pdf가 pptx를 그대로 받아들임). PDF→PPTX(역방향)는 범위 밖 — PDF→DOCX만큼도 대응되는 간단한 메커니즘이 없어 상당한 추가 엔지니어링이 필요, 요청되지 않음 | 2026-07-30 |
| DEC-017 | **DOCX→HWP 지원 추가**(REQ-F-005 개정) — DEC-003(HWP 쓰기 미지원)을 일부 번복. hwplib `BlankFileMaker`+`Paragraph.createText()`로 문단 텍스트를 생성해 새 사이드카(`sidecar/hwp/JsonToHwp.java`)로 HWP 파일을 만든다. **표는 지원 범위 밖** — 원본 DOCX의 표는 각 행을 `" \| "`로 이어붙인 텍스트 문단으로 단순화되어 저장된다(내용은 보존, 구조만 단순화, 변환 전 UI 고지). **자동 번호("1.")·불릿("•")은 numbering.xml을 해석해 문단 앞에 마커 텍스트로 붙여 보존**(직접 numPr·"List Number/Bullet" 스타일 경유 둘 다 처리, 다단계 중첩 재시작 규칙까지는 재현하지 않음) | 사용자 요청("네, 진행해줘"). hwplib에는 표를 처음부터 새로 만드는 도구·예시가 전혀 없음(저장소 전체 검색 확인) — 기존 `TableCopier`는 이미 읽어들인 표를 다른 파일로 복사하는 용도뿐이고, RESULT.md의 "쓰기 라운드트립 3/3"도 기존 표를 읽어 그대로 재저장한 결과이지 신규 생성 검증이 아니었음. 검증되지 않은 저수준 바이너리 표 컨트롤 조립을 시도하는 대신(DEC-015의 "검증되지 않은 개선보다 검증된 단순화" 원칙과 동일), 로컬 스파이크로 다중 문단 생성(BlankFileMaker 첫 문단 재사용 + `Section.addNewParagraph()`로 문단 추가)이 정상 동작함을 직접 확인(희귀 한글 자모·한자 포함 재읽기 100% 일치) 후 그 범위로 구현. 표는 " \| " 텍스트로 단순화해 내용 유실 없이 처리. 코드 리뷰(1차)에서 표 단순화가 변환 전 UI에 고지되지 않는다는 지적을 받아 전용 문구(`note.hwp_table_flatten`)를 추가하고 헤드리스 Qt 회귀 테스트로 고정. 코드 리뷰(2차)에서 DOCX 자동 번호·불릿(문단 텍스트가 아니라 numbering.xml 서식이라 별도 처리 없이는 조용히 사라짐)이 지적돼, `docx_extract.py`가 numbering.xml을 해석해 마커를 문단 텍스트에 합성하도록 보완(실제 생성 DOCX로 XML 구조 직접 확인 후 구현, 스타일 경유·직접 numPr 두 경로 모두 검증). CI에 JsonToHwp 컴파일·생성→재읽기 왕복 스모크 추가, 유닛 테스트(`test_pipeline.py::test_docx_to_hwp_roundtrip`·`test_docx_to_hwp_preserves_numbered_and_bullet_markers`, `test_converters.py::TestDocxExtractNumbering`)와 `scripts/smoke_pdf_pipeline.py`에도 전체 파이프라인 검증 추가 | 2026-07-30 |
| DEC-018 | **DOCX→HWP 문단 레이아웃(LineSeg) 재계산** — 문단 텍스트 길이에 맞춰 줄바꿈을 계산하고 줄마다 별도 `LineSegItem`을 생성, 문단이 이어질 때마다 세로 위치(`lineVerticalPosition`)를 누적한다(0으로 리셋하지 않음). `ParaHeader.lineAlignCount`도 실제 줄 수와 일치시킨다. 줄바꿈 폭 근사치(문자당 945 HWP 단위)는 실제 hwplib 샘플 문서에서 역산한 값 — 라틴 문자·구두점이 많이 섞이면 오차가 커질 수 있는 문서화된 한계 | 실사용 보고: "DOCX에서 HWP로 변환할 때 파일의 형태가 깨진다". 원인 진단: 기존 코드는 문단 길이와 무관하게 `LineSegItem`을 **항상 1개만**, `lineVerticalPosition`도 **모든 문단마다 0**으로 고정 생성하고 있었다. hwplib 샘플(`sample_hwp/distribution.hwp`)의 실제 여러 줄 문단을 직접 파싱해 대조한 결과, 실제 문서는 줄마다 별도 `LineSegItem`(textStartPosition·lineVerticalPosition이 줄마다 증가)을 갖고, `lineAlignCount`도 그 개수와 정확히 일치함을 확인 — 우리 코드가 "레이아웃 캐시"를 사실과 다르게 기록하고 있었던 것. 자체 검증(hwplib로 재읽기)은 텍스트 버퍼만 비교하므로 이 문제를 전혀 못 잡는다(텍스트는 항상 100% 일치로 나옴) — DEC-015 때와 같은 "우리 도구로 검증한 것과 실제 외부 프로그램이 보여주는 것은 다를 수 있다" 함정. **실제 한글/한워드 등 외부 뷰어에서의 최종 확인은 아직 못함**(Mac 개발 환경, 로컬에 HWP 뷰어 없음) — 구조적으로 실제 문서 패턴과 일치하도록 고쳤다는 것까지가 이번에 검증한 범위, 잔여 리스크로 아래 리스크 표에 기록. 코드 리뷰 지적("텍스트 왕복 테스트로는 이 문제를 못 잡는다")을 받아 테스트 전용 디버그 도구(`sidecar/hwp/LineSegDebug.java`, 배포용 엔진 번들에는 미포함)를 추가해 `lineAlignCount`·`LineSegItem` 개수·세로 위치 증가를 직접 검증하는 구조 테스트를 유닛 테스트와 CI 스모크(`build.yml`) 양쪽에 추가 | 2026-07-31 |
| DEC-019 | **XLSX→CSV 값 표시 형식 정리 + 다중 시트 고지** — 날짜(`datetime`)는 ISO 형식(`2026-07-31`, 자정이 아니면 시간 포함), 정수값으로 떨어지는 float(예: `3.0`)는 소수점 없이(`3`) 출력하도록 `_format_cell_value()` 추가. 워크북에 시트가 2개 이상이면 변환 전 UI에 "시트가 여러 개예요 — 첫 번째 시트만 변환돼요" 고지(`note.xlsx_multisheet`) 추가 — 여전히 첫 시트만 변환하지만 조용히 나머지를 버리지 않는다 | 메인 버전 진입 전 완성도 점검. 기존 코드는 openpyxl이 돌려주는 Python 객체를 `csv.writer`가 그대로 `str()`해서 `datetime(2026,7,31)`이 `"2026-07-31 00:00:00"`으로, 정수형 float이 `"3.0"`으로 나와 엑셀에서 보던 모습과 달랐음. 다중 시트는 `wb.active`(첫 시트)만 변환하면서 나머지 시트를 고지 없이 버리고 있었음. **여러 시트를 각각 파일로 출력하는 방안(B안)은 이번 라운드에서 보류** — 현재 데이터 모델(`FileItem.output`이 단일 Path, "입력 1개→출력 1개" 전제)을 바꿔야 하는 더 큰 작업이라 별도 과제로 분리. 코드 리뷰 지적으로 `wb.active`(파일이 마지막 저장 시점에 열려 있던 탭)와 UI 고지 문구("첫 번째 시트")가 실제로 다를 수 있음을 재현 확인(두 번째 시트를 활성 탭으로 저장한 실제 파일로 검증) — `wb.worksheets[0]`으로 고정해 고지 문구와 동작을 일치시킴. `_format_cell_value()`의 범위(통화·퍼센트 등 number_format 미반영, 날짜/정수 기본값 정규화만)도 docstring에 명시해 기대치를 좁힘 | 2026-07-31 |
| DEC-020 | **지원 안 되는 파일의 ✕(제거) 버튼이 클릭에 반응하지 않던 버그 수정** — `FileRow.__init__`이 지원 안 되는 파일에 대해 `self.setEnabled(False)`로 행 전체를 비활성화했는데, Qt에서는 부모가 비활성화되면 자식(`remove_btn`)에 `setEnabled(True)`를 호출해도 클릭이 전달되지 않는다(자식의 실질 활성 상태는 부모 상태와의 AND). 버튼은 눌러도 반응 없이 그대로 남아 있었음. 행 전체를 비활성화하는 대신, 회색으로 보여줄 개별 요소(아이콘·파일명·포맷 라벨)만 `setEnabled(False)`하고 `remove_btn`은 그대로 두도록 수정 | v0.3.3 실사용 Windows 테스터 리포트: "지원 안되는 파일 올리고 변환 시도한 후에 막히면 x버튼 클릭해도 안없어지더라". 헤드리스 Qt로 재현 확인(`remove_btn.isEnabled()`가 명시적 `setEnabled(True)` 이후에도 `False`로 나옴) 후 원인 특정, 수정 후 같은 방식으로 클릭 시뮬레이션 + 실제 앱(`MainWindow`) 종단 흐름으로 재검증. 회귀 테스트 추가(`tests/test_ui_filerow.py`) | 2026-07-31 |

## 리스크
| 리스크 | 심각도 | 완화책 |
|--------|:------:|--------|
| ~~HWP TXT/DOCX 오프라인 변환 품질~~ → **해소(2026-07-29)**: hwplib 스파이크 통과(공식 샘플) + 실사용 문서 검증(OQ-006, 공공기관 5건+hwplib 배포용 1건 총 6/6) 모두 통과. TXT/DOCX 범위에서 Must 유지 확정 (DEC-007·DEC-011) | ~~높음~~ → **낮음 (TXT/DOCX 한정)** | 1단계 기대치는 "텍스트+표 내용+기본 구조" 수준으로 고지(DEC-010). 사진·차트 중심 문서, 진짜 암호화 문서는 표본 밖 — 필요 시 추가 라운드 |
| **HWP→PDF 실사용 커버리지** — CI에서는 hwplib 샘플로 파이프라인 동작 확인(DEC-012), 공공기관 실사용 5건 기준 PDF 검증은 아직 별도 라운드로 남음 | 낮음 (기존 중간에서 하향) | v0.3b로 파이프라인 자체(HWP→DOCX→LibreOffice)는 CI 게이트로 매번 검증됨. OQ-006의 실사용 5건으로 PDF까지 재검증하는 것은 후속 research 라운드로 진행 |
| ~~LibreOffice 번들 라이선스(MPL)·재배포·패키징 복잡도~~ → **해소(2026-07-29)**: 버전 고정(26.2.5)·SHA256 검증·MPL 2.0 고지 포함 완료 (DEC-012) | ~~중간~~ → **낮음** | 잔여: 완전 클린 Windows(VC++ 런타임 없음)에서의 동작은 CI(러너에 이미 VC++ 런타임 존재)로는 완전히 검증되지 않음 — 실기기 검증 권장 |
| **완전 클린 Windows에서 LibreOffice 실행 미검증** (신규) | 중간 | GitHub Actions windows-latest 러너에는 VC++ 런타임이 이미 있어 CI 통과가 곧 "모든 사용자 PC에서 동작"을 보장하지 않음. 배포 전 VC++ 런타임이 없는 실제/가상 클린 Windows에서 1회 이상 수동 검증 권장 |
| PDF→DOCX 정확도에 대한 일반 사용자 기대와 기술 한계의 간극 | 중간 | 변환 전 안내 문구(OQ-003), 결과 요약에 한계 고지 |
| ~~PDF/HWP→DOCX 글자 깨짐(우리 앱 내부 렌더링)~~ → **해소(2026-07-30)**: Noto Sans KR 명시 지정으로 해결, 스모크로 폰트 임베딩까지 검증 (DEC-015). **단, 아래 항목대로 이 해소는 우리 앱 자신의 렌더링 경로에 한정** | ~~높음~~ → **낮음(우리 앱 경로 한정)** | — |
| **PDF/HWP→DOCX 결과물을 사용자의 Word/한글(HWP)에서 직접 열 때의 렌더링 — 미검증** (신규, 재리뷰에서 지적) | 중간 | `w:eastAsia="Noto Sans KR"`을 지정하지만 이 폰트는 우리 앱 안에만 번들되어 사용자의 별도 Word/한글에는 보이지 않는다 — 그 프로그램 자체의 글꼴 대체 로직에 의존. 대체로 "맑은 고딕"(Windows 기본, 상시 설치) 지정을 시도했으나 폰트 없는 환경에서 대체 로직이 오히려 더 나쁜 결과를 내는 회귀가 확인되어 채택 보류(DEC-015). 실제 Word/한글에서 열어보는 것은 CI로 검증 불가 — 다음 Windows 실사용자 테스트 항목에 포함(`testing/WINDOWS_TEST_GUIDE.md`) |
| **사용자가 직접 제공한 DOCX/PPTX(글꼴 미지정)의 DOCX→PDF 렌더링 — 호스트 글꼴 의존 잔존** | 중간 | 사용자가 가져온 기존 DOCX/PPTX는 그 파일이 지정한 글꼴(또는 무지정)을 그대로 따른다. LibreOffice의 폰트 대체 로직이 항상 번들 Noto Sans KR로 귀결되는 것은 아님을 로컬 검증 중 직접 확인. 대부분의 실제 한글 Word 문서는 맑은 고딕(Windows 기본 글꼴, 상시 설치됨)을 사용해 영향이 제한적일 것으로 추정되나 미검증. 필요 시 LibreOffice 폰트 대체 테이블(관공서 서식에 흔한 함초롬바탕 등 포함) 구성을 후속 작업으로 검토 |
| mac 개발 ↔ Windows 배포 간 폰트·경로·패키징 차이 | 중간 | 초기부터 Windows VM/실기기에서 주기적 검증 |
| **DOCX→HWP 생성 문서를 실제 한글/한워드 등 외부 뷰어에서 열 때의 렌더링 — 미검증** (DEC-018) | 중간 | LineSeg 레이아웃 캐시를 실제 문서 구조에 맞춰 재계산하도록 고쳤으나, 줄바꿈 폭 근사치(문자당 고정 폭)와 실제 폰트 메트릭 사이에는 오차가 있을 수 있고, 무엇보다 Mac 개발 환경에는 실제 한글 뷰어가 없어 최종 렌더링을 직접 확인하지 못했다. 구조가 실제 hwplib 샘플 문서 패턴과 일치함은 확인함(자체 검증의 한계). 다음 Windows 실사용자 테스트 라운드에 포함 필요 |
| XLSX→CSV 다중 시트 — 여전히 첫 시트만 변환(고지만 추가, DEC-019) | 낮음 | 여러 시트를 각각 파일로 출력하는 방안은 데이터 모델 변경이 필요해 별도 과제로 보류. 최소한 조용한 데이터 유실은 막음(변환 전 고지) |
