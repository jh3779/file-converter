# 06. 미해결 질문 · 가정 · 결정 로그

> 인터뷰 영역 8 + 진행 중 파생 · 상태: living document

## Open Questions (미결정)
| ID | 질문 | 영향 | 담당 | 필요 시점 |
|----|------|------|------|-----------|
| ~~OQ-001~~ | **해결됨 (2026-07-29)** — hwplib 기술 스파이크 통과 → DEC-007 확정. 상세: [spike/hwplib/RESULT.md](../spike/hwplib/RESULT.md) | REQ-F-005 · REQ-NF-004 | — | 완료 |
| ~~OQ-006~~ | **TXT·DOCX 한정 해결 (2026-07-29)** — 실사용 공공기관 HWP 5건 + hwplib 배포용 문서 1건, 총 6/6 TXT·DOCX 성공 → DEC-011. **HWP→PDF는 미검증 상태로 남음**(LibreOffice 필요, v0.3b 이후 재검증). 상세: [research/hwp-coverage/RESULT.md](../research/hwp-coverage/RESULT.md) | REQ-F-005 | — | TXT·DOCX 완료 / PDF는 v0.3b 이후 |
| ~~OQ-002~~ | ~~자동 업데이트 포함 여부~~ **해결됨(2026-08-03) — DEC-022 참고** | REQ-NF-002 | 기획 | v1 배포 전 |
| ~~OQ-003~~ | **해결됨 (2026-07-29)** — DOCX 대상 선택 시 행 인라인 고지 문안 확정 → DEC-010 | REQ-F-004 · SCR-001 | — | 완료 |
| OQ-004 | 최근 기록 보관 건수 N과 저장 방식(SQLite vs JSON) | ENT-003 | 개발자 | 구현 시 |
| ~~OQ-005~~ | **해결됨 (2026-07-29)** — 결과 화면은 SCR-001 위 오버레이 카드로 확정 → DEC-008 | SCR-002 | — | 완료 |

## 가정 (Assumptions)
잠정으로 두고 진행한 전제. 틀리면 재검토 대상.
| ID | 가정 | 근거/리스크 |
|----|------|-------------|
| ASM-001 | 번들 LibreOffice 엔진으로 DOCX→PDF 정확도가 일반 사용자 기대치를 충족한다 | 복잡한 서식·한글 글꼴에서 차이 가능. CI는 기계적 변환 성공만 검증(DEC-012) — 육안 품질 검증은 별도 필요 |
| ASM-002 | 설치 용량 수백 MB는 "설치 간편함" 요구와 상충하지 않는다 (사용자가 택한 것은 '별도 프로그램 불요'이지 '소용량'이 아님) | 인터뷰에서 엔진 번들 선택으로 확인됨 |
| ASM-003 | 대상 파일 크기는 일반 사무 문서 수준(수십 MB 이하) — 초대형 파일 최적화는 MVP 범위 밖 | 대용량 CSV/PDF에서 성능 이슈 시 재검토 |
| ~~ASM-004~~ | ~~UI 언어는 한국어 우선~~ → DEC-009로 대체(한국어+영어 2개 언어 확정) | — |

## 결정 로그 (DEC-*)
확정된 결정과 근거. 임의 번복 금지.
| ID | 결정 | 근거 | 날짜 |
|----|------|------|------|
| DEC-001 | 기술 스택: **Python + PySide6(Qt) + PyInstaller 패키징 + LibreOffice 엔진 번들** | 변환 라이브러리 생태계가 가장 풍부한 단일 스택(openpyxl/pandas: CSV·XLSX·JSON, pdfminer/pdf2docx: PDF, pyhwp: HWP). 어차피 LibreOffice를 번들하므로(수백 MB) Python 런타임 용량은 무의미. Tauri/Electron은 변환용 Python 사이드카가 별도로 필요해 2중 스택이 됨. Qt는 mac 개발→Windows 배포 크로스 플랫폼 충족 | 2026-07-29 |
| DEC-002 | DOCX→PDF는 번들 LibreOffice headless 엔진 사용 (경량 자체 변환 대신) | 정확도 우선. "별도 프로그램 설치 불요" 조건은 번들로 충족 | 2026-07-29 |
| ~~DEC-003~~ | ~~HWP는 읽기 전용 (HWP→PDF/TXT/DOCX만, HWP 쓰기 미지원)~~ **DEC-017로 일부 번복** — DOCX→HWP(문단 텍스트) 지원 추가 | 오프라인 단독 구현의 현실적 범위 | 2026-07-29 |
| DEC-004 | 출력은 원본 폴더 + 이름 충돌 시 자동 리네임, 원본 절대 비수정 | 원본 보호 품질 기준. 저장 위치 선택 UI는 MVP 제외 | 2026-07-29 |
| DEC-005 | 일괄 변환 실패 시 계속 진행 + 결과 요약 | 대량 처리 흐름 유지 | 2026-07-29 |
| DEC-006 | 메인 UX는 원스크린 드롭존 (마법사·카테고리 방식 배제) | 3클릭 완결 단순함 | 2026-07-29 |
| DEC-007 | HWP 처리는 **hwplib**(Apache-2.0, main 브랜치 소스 빌드) + JRE 사이드카로 확정. pyhwp(AGPL)는 사용 금지. HWP→PDF는 hwplib 구조 추출 → DOCX 생성 → LibreOffice 파이프라인 | 스파이크 통과: 읽기 11/11, 표 셀·한글·수식 추출 정상, 193만 자 189ms, 쓰기 라운드트립 3/3 및 신규 생성 검증. 외부 의존성 0(POI shaded). 상세: [spike/hwplib/RESULT.md](../spike/hwplib/RESULT.md) | 2026-07-29 |
| DEC-008 | 결과 화면(SCR-002)은 별도 창이 아닌 **SCR-001 위 오버레이 카드** | 와이어프레임 S-04 사용자 승인. 단일 창 유지·컨텍스트 보존 | 2026-07-29 |
| DEC-009 | UI 언어는 **한국어·영어 2개만** 지원. 기본은 시스템 언어 감지(한국어면 ko, 그 외 en), 설정 메뉴에서 수동 전환 가능 | 사용자 확정. 1차 사용자는 국내 일반인, 2차 확장 대비 영어 | 2026-07-29 |
| DEC-010 | PDF/HWP→DOCX 기대치 고지: 대상 포맷 DOCX 선택 시 행 인라인 캡션 "레이아웃이 단순화될 수 있어요 — 텍스트·표 내용은 유지됩니다" | 레이아웃 완전 보존은 기술적 불가(OQ-003). pdf2docx(PyMuPDF)는 AGPL이라 배제 — 텍스트/구조 기반 변환 + 정직한 고지 | 2026-07-29 |
| DEC-011 | HWP 지원 범위 중 **TXT/DOCX만** 실사용 문서 기준으로도 Must 유지 확정 (HWP→PDF는 미검증 — 별도 잔여 리스크). "배포용(복사방지)" 문서는 실패 케이스로 별도 취급하지 않음(TXT·DOCX 양쪽 경로 모두 일반 경로로 정상 처리됨을 확인) | 공공기관 실사용 서식 5건 + hwplib 배포용 샘플 1건, 6/6 TXT·DOCX 성공·인코딩 깨짐 0건 검증(distribution.hwp도 DOCX 성공, 문단 47·표 1). HWP→PDF는 로컬 LibreOffice 부재로 6/6 err.engine_missing — 검증 안 됨을 "품질 낮음"과 혼동하지 않음. err.password 분기는 진짜 암호화 파일 대비 방어적으로만 유지 | 2026-07-29 |
| DEC-012 | LibreOffice 번들 방식 확정: **버전 26.2.5 고정 + 공식 서버 SHA256 검증 후 다운로드 → MSI 관리자 설치(`msiexec /a`)로 시스템 설치·레지스트리 등록 없이 파일만 추출 → `engine/libreoffice/`에 동봉**. CI가 다운로드 캐시(버전+체크섬 키) + 체크섬 검증 + DOCX→PDF·HWP→PDF 전체 파이프라인 스모크(패키징 후에는 `--frozen-exe`로 실제 exe 실행 시 쓰이는 `engine_dir()` 자동 탐색 자체를 환경변수 없이 검증, PDF 내용까지 pdfminer로 확인)를 매 PR마다 게이트로 실행 — **CI 전체 그린 확인 완료** ([실행 기록](https://github.com/jh3779/file-converter/actions/runs/30458685792)) | hwplib 리비전 고정(DEC-007) 및 CI 게이트 승격(PR #2 리뷰) 원칙과 동일하게 적용 — 임의 최신 버전 다운로드 대신 재현 가능한 고정 버전·체크섬. 관리자 설치는 실제 설치·부팅 시 서비스 등록 없이 파일 트리만 얻는 표준 MSI 기능. `office.py`·`hwp.py`는 PR #2부터 이미 `engine_dir()` 기반 자동 탐색을 구현하고 있었음(PR #4에 반복 제기된 재리뷰에서 "탐색 못 함"·"PR 게이트 미실행" 지적이 있었으나, 현재 커밋·실제 CI 실행 기록으로 직접 반증됨 — 다만 스모크가 자동 탐색 자체를 검증하지 않고 항상 환경변수로 우회하던 것은 그 안의 유효한 지적이라 `--frozen-exe` 시뮬레이션으로 보강, CI에서 실제 통과 확인). 이 과정에서 진짜 결함 하나를 CI로 발견해 수정: JVM 사이드카에 한글 경로("표.hwp")를 그대로 넘기면 영어 로캘 Windows(en-US 러너)에서 `sun.jnu.encoding` 문제로 `?`로 깨져 실패 — ASCII 별칭 경로 우회로 근본 수정, 이후 재실행 통과. 알려진 잔여 제약: 완전 클린 Windows(VC++ 런타임 전무)에서의 동작은 미검증 — 실기기 검증 권장 | 2026-07-29 |
| DEC-013 | v0.3c Windows 인스톨러 확정: **Inno Setup**(무료·MPL 호환 재배포) + `PrivilegesRequired=lowest`(관리자 권한·UAC 불요, 사용자 폴더 설치) + 한/영 설치 언어(Korean.isl/기본) + 앱 아이콘 신규 제작(Vault Teal 브랜드, PIL로 생성) + 제3자 고지는 EULA 강제 동의가 아닌 정보 페이지(InfoBeforeFile)로 표시 + 버전 단일 소스(`packaging/VERSION`). CI가 설치→설치된 실경로에서 exe 실행→무인 제거까지 전 과정을 매 PR 게이트로 검증 | 비개발자 대상 도구는 zip 압축 해제보다 원클릭 설치가 REQ-NF-006(3클릭) 철학에 부합. WiX/MSI는 엔터프라이즈 배포 기능이 필요 없는 개인용 도구에 과함. UAC 프롬프트는 비개발자에게 불필요한 마찰이라 기본 배제(엔터프라이즈 전역 배포가 필요해지면 재검토). 자동 업데이트 체크는 넣지 않음(REQ-NF-002·OQ-002 미결 상태 유지) | 2026-07-29 |
| DEC-014 | ~~README 다운로드 링크는 **Windows만 제공, macOS는 소스 실행 안내, Linux는 미지원 명시**~~ → **macOS도 다운로드 링크 제공으로 정정(2026-08-05, DEC-029)** — 아래 macOS 배포판 보류 사유가 뒤집힘. Windows 링크는 GitHub Release 자산 — 태그(`v*`) push 시 CI가 인스톨러를 빌드해 버전별 파일명과 고정 파일명(`FileConverter-Setup-latest.exe`) 두 가지로 릴리스에 첨부. 0.x 버전이라 `--prerelease`로 표시. README는 **태그가 명시된 직접 링크**(`.../releases/download/vX.Y.Z/FileConverter-Setup-latest.exe`)를 쓰고 버전 태그 시 함께 갱신 | macOS 배포판(.app/.dmg, 코드 서명·공증)은 신규 엔지니어링 범위이고 REQ-NF-001(macOS는 개발 환경)과 충돌해 사용자에게 직접 확인 후 보류 결정. Actions 아티팩트(90일 만료, 로그인 필요)는 README의 영구 다운로드 링크로 부적합해 정식 GitHub Release로 전환. **최초 시도했던 `.../releases/latest/download/...` 단축 링크는 v0.3.0 발행 직후 404로 실패 확인** — GitHub이 pre-release를 "latest"로 인정하지 않기 때문(`gh api repos/.../releases/latest`도 인증 상태로 404 재현). `--prerelease`(정직한 라벨링, 0.x 상태 반영)를 유지하는 대신 태그 직접 링크로 전환해 실제 동작을 authenticated API로 재확인함 | 2026-07-29 |
| DEC-015 | 실사용 테스트에서 보고된 두 글자 깨짐 버그의 근본 원인 확정 및 수정: **① `blocks_to_docx`(PDF→DOCX·HWP→DOCX 출력)가 한글 글꼴을 전혀 지정하지 않았음**(rFonts 0건 확인) → 모든 run에 "Noto Sans KR"을 `w:ascii`/`w:eastAsia`/`w:cs`로 명시. **② 번들 LibreOffice 자체 글꼴 118개 중 한글/CJK가 0개** → Noto Sans KR(Regular·Bold, OFL-1.1)을 `engine/libreoffice/share/fonts/truetype/`에 동봉(LO가 자체 스캔하는 경로, 시스템 글꼴 설치 불요). **이 보장은 우리 앱 자신의 렌더링 경로(DOCX/HWP→PDF)에 한정** — 결과 DOCX를 사용자가 자신의 Word/한글(HWP)에서 직접 열 때는 그 프로그램에 Noto Sans KR이 없으면 별도 대체가 일어난다(재리뷰에서 지적됨, 정당한 지적). 대안으로 Windows에 사실상 항상 있는 "맑은 고딕"을 1순위로 지정하는 것을 시도했으나, 그 폰트가 없는 환경(mac)에서 LibreOffice 대체 로직이 무지정 상태보다 더 나쁜 대체를 골라 희귀 글자(뷁) 매핑이 깨지는 회귀가 실측 확인되어 **채택하지 않고 검증된 Noto Sans KR 단일 지정으로 유지** | 로컬에 LibreOffice 26.2.5(Windows 번들과 동일 버전) 설치 후 실제 파이프라인으로 재현·검증: (1) PDF→DOCX 글자 깨짐 → 원인은 글꼴 미지정, 텍스트 추출 자체는 정상이었음(재현 중 오판 1회 있었음 — LibreOffice의 PDF→PNG 자체 렌더링 경로가 별도로 손상되어 있었고 이를 실제 버그로 오인했으나, macOS QuickLook(CoreGraphics)으로 재검증해 PDF 자체는 정상임을 확인). (2) DOCX→PDF 글자 삭제 → 번들 LO의 CJK 글꼴 부재가 근본 원인. 수정 후 폰트 임베딩을 스모크 게이트로 확인 — 최초엔 `b"NotoSansKR" in raw` 단순 문자열 검사였으나(이름 참조만 있고 실제 미내장이어도 통과할 수 있어 불충분하다는 재리뷰 지적을 반영), pdfminer로 PDF 객체를 직접 파싱해 `/FontDescriptor`의 `/FontFile`·`/FontFile2`·`/FontFile3` 실제 존재까지 확인하도록 강화(양성/음성 케이스 로컬 단위 검증 완료 — 예: Base14 `Helvetica` 이름 참조는 미내장으로 정확히 판별). PPTX에도 폰트 명시 지정 케이스를 추가해 동일하게 엄격 검증. (3) 맑은 고딕 대체 시도 → 로컬 재검증에서 회귀 발견, 원복 | 2026-07-30 |
| DEC-016 | **PPTX→PDF 지원 추가**(REQ-F-012) — 기존 `office.py`의 `office_to_pdf`(구 `docx_to_pdf`, 개명)를 그대로 재사용. LibreOffice `--convert-to`는 입력 포맷을 자동 감지하므로 별도 파이프라인 불필요 | 사용자 요청. PPTX→PDF는 DOCX→PDF와 동일하게 LibreOffice Impress 컴포넌트가 네이티브로 처리하며, 코드 변경 없이 로컬 검증 완료(soffice --convert-to pdf가 pptx를 그대로 받아들임). PDF→PPTX(역방향)는 범위 밖 — PDF→DOCX만큼도 대응되는 간단한 메커니즘이 없어 상당한 추가 엔지니어링이 필요, 요청되지 않음 | 2026-07-30 |
| DEC-017 | **DOCX→HWP 지원 추가**(REQ-F-005 개정) — DEC-003(HWP 쓰기 미지원)을 일부 번복. hwplib `BlankFileMaker`+`Paragraph.createText()`로 문단 텍스트를 생성해 새 사이드카(`sidecar/hwp/JsonToHwp.java`)로 HWP 파일을 만든다. ~~**표는 지원 범위 밖** — 원본 DOCX의 표는 각 행을 `" \| "`로 이어붙인 텍스트 문단으로 단순화되어 저장된다~~ → **DEC-028로 정정: "hwplib에는 표를 처음부터 만드는 도구가 전혀 없다"는 아래 근거란의 전제가 틀렸음이 확인됨 — 표는 이제 실제 HWP 표 컨트롤로 새로 생성된다**(내용은 보존, 구조만 단순화, 변환 전 UI 고지). **자동 번호("1.")·불릿("•")은 numbering.xml을 해석해 문단 앞에 마커 텍스트로 붙여 보존**(직접 numPr·"List Number/Bullet" 스타일 경유 둘 다 처리, 다단계 중첩 재시작 규칙까지는 재현하지 않음) | 사용자 요청("네, 진행해줘"). hwplib에는 표를 처음부터 새로 만드는 도구·예시가 전혀 없음(저장소 전체 검색 확인) — 기존 `TableCopier`는 이미 읽어들인 표를 다른 파일로 복사하는 용도뿐이고, RESULT.md의 "쓰기 라운드트립 3/3"도 기존 표를 읽어 그대로 재저장한 결과이지 신규 생성 검증이 아니었음. 검증되지 않은 저수준 바이너리 표 컨트롤 조립을 시도하는 대신(DEC-015의 "검증되지 않은 개선보다 검증된 단순화" 원칙과 동일), 로컬 스파이크로 다중 문단 생성(BlankFileMaker 첫 문단 재사용 + `Section.addNewParagraph()`로 문단 추가)이 정상 동작함을 직접 확인(희귀 한글 자모·한자 포함 재읽기 100% 일치) 후 그 범위로 구현. 표는 " \| " 텍스트로 단순화해 내용 유실 없이 처리. 코드 리뷰(1차)에서 표 단순화가 변환 전 UI에 고지되지 않는다는 지적을 받아 전용 문구(`note.hwp_table_flatten`)를 추가하고 헤드리스 Qt 회귀 테스트로 고정. 코드 리뷰(2차)에서 DOCX 자동 번호·불릿(문단 텍스트가 아니라 numbering.xml 서식이라 별도 처리 없이는 조용히 사라짐)이 지적돼, `docx_extract.py`가 numbering.xml을 해석해 마커를 문단 텍스트에 합성하도록 보완(실제 생성 DOCX로 XML 구조 직접 확인 후 구현, 스타일 경유·직접 numPr 두 경로 모두 검증). CI에 JsonToHwp 컴파일·생성→재읽기 왕복 스모크 추가, 유닛 테스트(`test_pipeline.py::test_docx_to_hwp_roundtrip`·`test_docx_to_hwp_preserves_numbered_and_bullet_markers`, `test_converters.py::TestDocxExtractNumbering`)와 `scripts/smoke_pdf_pipeline.py`에도 전체 파이프라인 검증 추가 | 2026-07-30 |
| DEC-018 | **DOCX→HWP 문단 레이아웃(LineSeg) 재계산** — 문단 텍스트 길이에 맞춰 줄바꿈을 계산하고 줄마다 별도 `LineSegItem`을 생성, 문단이 이어질 때마다 세로 위치(`lineVerticalPosition`)를 누적한다(0으로 리셋하지 않음). `ParaHeader.lineAlignCount`도 실제 줄 수와 일치시킨다. 줄바꿈 폭 근사치(문자당 945 HWP 단위)는 실제 hwplib 샘플 문서에서 역산한 값 — 라틴 문자·구두점이 많이 섞이면 오차가 커질 수 있는 문서화된 한계 | 실사용 보고: "DOCX에서 HWP로 변환할 때 파일의 형태가 깨진다". 원인 진단: 기존 코드는 문단 길이와 무관하게 `LineSegItem`을 **항상 1개만**, `lineVerticalPosition`도 **모든 문단마다 0**으로 고정 생성하고 있었다. hwplib 샘플(`sample_hwp/distribution.hwp`)의 실제 여러 줄 문단을 직접 파싱해 대조한 결과, 실제 문서는 줄마다 별도 `LineSegItem`(textStartPosition·lineVerticalPosition이 줄마다 증가)을 갖고, `lineAlignCount`도 그 개수와 정확히 일치함을 확인 — 우리 코드가 "레이아웃 캐시"를 사실과 다르게 기록하고 있었던 것. 자체 검증(hwplib로 재읽기)은 텍스트 버퍼만 비교하므로 이 문제를 전혀 못 잡는다(텍스트는 항상 100% 일치로 나옴) — DEC-015 때와 같은 "우리 도구로 검증한 것과 실제 외부 프로그램이 보여주는 것은 다를 수 있다" 함정. **실제 한글/한워드 등 외부 뷰어에서의 최종 확인은 아직 못함**(Mac 개발 환경, 로컬에 HWP 뷰어 없음) — 구조적으로 실제 문서 패턴과 일치하도록 고쳤다는 것까지가 이번에 검증한 범위, 잔여 리스크로 아래 리스크 표에 기록. 코드 리뷰 지적("텍스트 왕복 테스트로는 이 문제를 못 잡는다")을 받아 테스트 전용 디버그 도구(`sidecar/hwp/LineSegDebug.java`, 배포용 엔진 번들에는 미포함)를 추가해 `lineAlignCount`·`LineSegItem` 개수·세로 위치 증가를 직접 검증하는 구조 테스트를 유닛 테스트와 CI 스모크(`build.yml`) 양쪽에 추가 | 2026-07-31 |
| DEC-019 | **XLSX→CSV 값 표시 형식 정리 + 다중 시트 고지** — 날짜(`datetime`)는 ISO 형식(`2026-07-31`, 자정이 아니면 시간 포함), 정수값으로 떨어지는 float(예: `3.0`)는 소수점 없이(`3`) 출력하도록 `_format_cell_value()` 추가. 워크북에 시트가 2개 이상이면 변환 전 UI에 "시트가 여러 개예요 — 첫 번째 시트만 변환돼요" 고지(`note.xlsx_multisheet`) 추가 — 여전히 첫 시트만 변환하지만 조용히 나머지를 버리지 않는다 | 메인 버전 진입 전 완성도 점검. 기존 코드는 openpyxl이 돌려주는 Python 객체를 `csv.writer`가 그대로 `str()`해서 `datetime(2026,7,31)`이 `"2026-07-31 00:00:00"`으로, 정수형 float이 `"3.0"`으로 나와 엑셀에서 보던 모습과 달랐음. 다중 시트는 `wb.active`(첫 시트)만 변환하면서 나머지 시트를 고지 없이 버리고 있었음. **여러 시트를 각각 파일로 출력하는 방안(B안)은 이번 라운드에서 보류** — 현재 데이터 모델(`FileItem.output`이 단일 Path, "입력 1개→출력 1개" 전제)을 바꿔야 하는 더 큰 작업이라 별도 과제로 분리. 코드 리뷰 지적으로 `wb.active`(파일이 마지막 저장 시점에 열려 있던 탭)와 UI 고지 문구("첫 번째 시트")가 실제로 다를 수 있음을 재현 확인(두 번째 시트를 활성 탭으로 저장한 실제 파일로 검증) — `wb.worksheets[0]`으로 고정해 고지 문구와 동작을 일치시킴. `_format_cell_value()`의 범위(통화·퍼센트 등 number_format 미반영, 날짜/정수 기본값 정규화만)도 docstring에 명시해 기대치를 좁힘 | 2026-07-31 |
| DEC-020 | **지원 안 되는 파일의 ✕(제거) 버튼이 클릭에 반응하지 않던 버그 수정** — `FileRow.__init__`이 지원 안 되는 파일에 대해 `self.setEnabled(False)`로 행 전체를 비활성화했는데, Qt에서는 부모가 비활성화되면 자식(`remove_btn`)에 `setEnabled(True)`를 호출해도 클릭이 전달되지 않는다(자식의 실질 활성 상태는 부모 상태와의 AND). 버튼은 눌러도 반응 없이 그대로 남아 있었음. 행 전체를 비활성화하는 대신, 회색으로 보여줄 개별 요소(아이콘·파일명·포맷 라벨)만 `setEnabled(False)`하고 `remove_btn`은 그대로 두도록 수정 | v0.3.3 실사용 Windows 테스터 리포트: "지원 안되는 파일 올리고 변환 시도한 후에 막히면 x버튼 클릭해도 안없어지더라". 헤드리스 Qt로 재현 확인(`remove_btn.isEnabled()`가 명시적 `setEnabled(True)` 이후에도 `False`로 나옴) 후 원인 특정, 수정 후 같은 방식으로 클릭 시뮬레이션 + 실제 앱(`MainWindow`) 종단 흐름으로 재검증. 회귀 테스트 추가(`tests/test_ui_filerow.py`) | 2026-07-31 |
| DEC-021 | **일괄 변환 시 동시 완료로 결과물이 조용히 덮어써질 수 있던 경쟁 상태(race condition) 수정** — `output.finalize()`가 "이름 충돌 확인 → 파일 이동"을 원자적으로 처리하지 않아, 같은 stem+확장자로 끝나는 두 파일이 QThreadPool(최대 4개 동시)에서 거의 동시에 완료되면 둘 다 같은 "충돌 없음" 경로를 계산해버려 한쪽이 다른 쪽 결과물을 조용히 덮어썼다 — REQ-F-008/INV-01·05("덮어쓰기 경로 없음") 위반. 이름 예약과 이동을 `threading.Lock`으로 감싼 하나의 임계 구역으로 묶어 수정. 같은 감사에서 `workers.py`의 `Job._one_finished()`(여러 스레드가 동시 호출하는 카운터 감소)도 이론상 원자성 보장이 없어(파이썬 GIL 구현 세부사항에 의존할 뿐 언어 스펙 아님) 방어적으로 같은 방식의 락 적용 | "실사용 테스트에서 발견 안 된 숨은 오류가 있을 수 있으니 확인해 달라"는 사용자 요청으로 진행한 코드 감사. `finalize()` 레이스는 4개 스레드로 동시성 스트레스 테스트를 직접 작성해 재현(30회 중 실제로 파일이 사라지는 결과 확인) 후 수정, 락 적용 후 같은 스트레스 테스트로 재발 없음 확인. `Job._one_finished()`의 카운터 레이스는 공격적인 스트레스 테스트(스레드 배리어로 동시 시작 강제 + `sys.setswitchinterval` 최소화)로도 재현하지 못했지만(CPython GIL 특성상 이 정도 단순 연산은 실질적으로 거의 항상 안전), 언어 스펙이 보장하는 게 아니라는 원칙에 따라 비용이 거의 없는 방어적 수정을 함께 적용 — 재현 못 한 것은 명확히 구분해 문서화(과장 금지) | 2026-07-31 |
| DEC-022 | **OQ-002 해결: 업데이트 확인 기능 추가, 옵트인·기본 꺼짐** — 설정(⚙) 메뉴에 체크 가능한 "업데이트 확인" 토글 추가(`app/update_check.py`). 켜져 있을 때만 앱 시작 시·토글을 켜는 순간 GitHub Releases API(`/repos/jh3779/file-converter/releases`)에 **버전 번호만** 조회한다 — 파일 내용·경로·사용자 식별 정보는 전송하지 않고, 자동 다운로드·설치도 하지 않는다(새 버전이 있으면 조용한 안내 문구만 표시, 클릭 시 릴리스 페이지를 브라우저로 열 뿐). 오프라인이거나 요청이 실패하면 예외를 던지지 않고 조용히 무시(선택 기능의 실패가 필수 기능 오류처럼 보이면 안 됨). 앱이 자기 버전을 알아야 해서 `app/version.py`를 신설 — `packaging/VERSION`(기존 단일 정본, DEC-013)을 그대로 읽어 값을 복제하지 않음: 개발 환경은 저장소의 파일을 직접, 배포판(frozen)은 CI가 exe 옆에 복사해 둔 사본을 읽는다(PyInstaller가 임의 파일을 자동으로 담지 않으므로) | 사용자 요청("자동 업데이트 안내 기능도 추가... 확인만 인터넷 연결시 하면 될 것 같은데")에 대해 옵트인·조회 전용으로 좁혀서 진행할지 의견을 먼저 구했고("완전 오프라인"을 내세운 도구에서 기본으로 조용히 네트워크를 타는 것은 신뢰 문제) 동의를 받은 뒤 그 범위로 구현. 버전 비교는 문자열이 아닌 튜플로(예: "0.3.10" > "0.3.4"를 문자열 비교하면 틀리게 나옴 — 테스트로 직접 확인). 네트워크 실패·빈 응답·JSON 파싱 실패 각각을 목(mock)으로 테스트해 예외가 절대 밖으로 안 나가는지 확인. CI에 번들된 VERSION이 `packaging/VERSION`과 일치하는지 검증하는 스텝과, `--frozen-exe` 시뮬레이션에 버전 자동 탐색 확인을 추가 | 2026-08-03 |
| DEC-023 | **PDF→HWP 경로 추가**(REQ-F-005 확장) — PDF는 pdfminer로 텍스트만 추출하고(PDF→DOCX의 DEC-010과 동일 파이프라인), 그 텍스트를 문단 블록으로 만들어 기존 `JsonToHwp` 사이드카로 HWP를 생성한다. PDF 자체가 표 구조를 담고 있지 않으므로(추출 결과가 이미 평문) DOCX→HWP(DEC-017)처럼 표를 별도로 단순화할 필요가 없다 — 코드는 `docx_to_hwp`·`pdf_to_hwp`가 공통 `_blocks_to_hwp` 헬퍼를 공유하도록 정리 | 사용자 요청("docs에서 hwp로 그리고 hwp에서 docs로... pdf에서 hwp로도"). 확인해보니 DOCX→HWP(DEC-017)·HWP→DOCX는 이미 구현돼 있었고 PDF→HWP만 없었음 — 사용자에게 확인 후 그 경로만 추가. UI 고지는 PDF/HWP→DOCX와 같은 `note.simplified`를 재사용(레이아웃 단순화라는 성격이 같음, DOCX→HWP 전용의 `note.hwp_table_flatten`과는 다름 — PDF엔 애초에 표 구조가 없어서). 실제 미니 PDF로 왕복 검증(유닛 테스트 추가) | 2026-08-03 |
| DEC-024 | **영상→MP4 변환 신규 지원**(REQ-F-014) — FFmpeg LGPL 빌드(BtbN/FFmpeg-Builds, GPL 전용 라이브러리 미포함) 번들. **영상 스트림은 H.264/HEVC일 때만 재인코딩 없이 스트림 카피**(무손실·즉시 완료) — 그 외 코덱은 v1 범위 밖으로 명시적으로 거부(`err.video_codec_unsupported`). 오디오는 AAC면 카피, 아니면 FFmpeg 내장 AAC 인코더(외부 라이브러리 불필요)로만 재인코딩. 자막 트랙은 드롭(v1 미지원). 오디오 트랙은 전부 보존(트랙별 독립 판단). 입력 확장자: avi/mov/mkv/wmv/flv/m4v — webm은 제외(표준 WEBM은 VP8/VP9/AV1만 담아 이 범위와 사실상 항상 불일치, 코드 리뷰로 발견해 제거). 첨부 이미지(커버 아트)가 실제 영상보다 앞선 파일에서 잘못된 스트림이 매핑될 수 있던 버그도 같은 리뷰에서 발견해 절대 스트림 인덱스 매핑으로 수정 | 사용자 요청("영상 확장자에 있어 mp4로 변환해주는것도 해줘") → 구현 전 전수조사 진행. **1차 조사**: FFmpeg 기본 빌드는 LGPL로 안전하지만 H.264 인코더 libx264는 GPL — 대안으로 Cisco OpenH264(BSD)를 검토. **2차 조사(사용자가 "정말 최적인지 다시 확인해달라"고 요청해 재검토)**: OpenH264 실사용 화질이 x264보다 뚜렷이 나쁨을 Cisco 자체 GitHub 이슈로 확인(추천 철회). 이 과정에서 재인코딩 없이 컨테이너만 바꾸는 "스트림 카피" 방식을 발견 — 로컬에서 원본과 결과물의 영상 스트림 MD5가 완전히 일치함을 직접 검증(무손실). ffmpeg가 exit 0으로 "성공"해도 MP4 표준에 안 맞는 조합(MPEG-2나 AC3를 그대로 담은 MP4)을 만들 수 있음을 재현 확인 — exit code가 아니라 ffprobe로 코덱명을 명시적으로 확인해야 함. **3차 조사(사용자 요청으로 계속 진행)**: 오디오 코덱까지 포함해 재검증 — 스트림별로(영상/오디오 각각) 카피 여부를 독립적으로 결정해야 함을 발견(예: H.264+AC3는 영상만 카피, 오디오만 AAC 재인코딩). 이 설계로 좁히니 실사용 영상 대부분(이미 H.264/HEVC인 폰 영상·화면 녹화 등)이 영상 인코더 자체가 필요 없어져, OpenH264/x264(GPL)/h264_mf(Windows 전용, 테스트 불가) 중 하나를 고르는 문제 자체가 v1에서 사라짐 — 검증 안 된 복잡한 선택지보다 검증된 좁은 범위(DEC-010·017과 같은 원칙). CI 스모크용 실제 H.264+AAC 샘플을 저장소에 추가(`sidecar/ffmpeg/sample_h264_aac.mov`) — 번들 LGPL 빌드는 GPL 인코더가 없어 스스로 H.264를 만들 수 없어서 필요. `--frozen-exe` 시뮬레이션에 ffmpeg/ffprobe 자동 탐색 확인 추가 | 2026-08-04 |
| DEC-025 | **일반 이미지 포맷 변환 신규 지원**(REQ-F-015) — JPG/JPEG/PNG/BMP/GIF/WEBP/TIFF 상호 변환. Pillow(HPND 라이선스, permissive) 사용 — 새 네이티브 바이너리 번들 불필요(`requirements.txt` 추가만으로 PyInstaller가 포함). EXIF Orientation을 반영해 회전된 사진이 그대로 저장되도록 하고, 알파 채널이 있는 이미지를 JPG/BMP처럼 투명을 지원하지 않는 포맷으로 저장할 때는 흰 배경으로 합성한다. 애니메이션 GIF/WEBP는 대상 포맷이 애니메이션을 지원하더라도 항상 첫 프레임만 사용(일관성 있는 단순 동작을 위한 의도적 범위 제한) — 변환 전 UI에 고지(`note.image_first_frame`) | "메인 버전"을 향한 사용자 요청 — 4-phase 계획(이미지 변환·PDF→이미지·문서 서식 충실도) 중 가장 작은 phase부터 착수. jpg/jpeg는 같은 포맷(JPEG)으로 취급해 서로를 자기 자신의 대상 목록에서 제외(TARGETS 원칙 — "가능한 것만 노출"). | 2026-08-05 |
| DEC-026 | **PDF→이미지 신규 지원**(REQ-F-016) — 원본 파일명과 같은 이름의 폴더를 만들어 그 안에 페이지별 PNG(`page_1.png`, `page_2.png`, …, 10페이지 이상이면 자릿수에 맞춰 0-패딩)를 저장한다. 렌더링 엔진은 **pypdfium2**(Apache-2.0/BSD-3-Clause, permissive — Google이 Chromium용으로 공개한 PDFium의 파이썬 바인딩) 채택. **이 phase가 최초로 "출력 파일 1개" 가정을 깨는 사례**라 `app/output.py`(`unique_output_path`/`finalize`가 `ext=None`이면 확장자 없이 폴더째로 충돌 검사·이동하도록 확장 — `shutil.move`는 폴더에도 그대로 동작해 별도 분기 불필요), `main_window.py`의 결과 폴더 열기 버튼과 기록(History) "위치 열기" 버튼 둘 다 결과가 폴더면 그 폴더 자체를, 파일이면 부모 폴더를 열도록 수정 | 사용자 요청("pdf에서 이미지 변환... 파일명으로 폴더 생성 후 페이지별 이미지")과 뒤이은 논의. **엔진 조사**: 이미 번들된 LibreOffice로 새 의존성 없이 해결되는지 먼저 실측 — `soffice --headless --convert-to png`가 다중 페이지 PDF에서 **첫 페이지 1장만** 내보내는 것을 로컬에서 직접 확인(3페이지 PDF → PNG 1개)해 기각. 사용자가 직접 제안한 "자체 렌더링 엔진 설계"는 PDF 래스터화가 사실상 PDF 렌더링 엔진을 새로 만드는 일(벡터 그래픽·임베디드 폰트·색공간·투명도 전부 처리해야 함)이라 검토 후 반대 — 대신 Google Chrome이 실사용하는 PDFium을 pip 설치만으로 쓸 수 있는 pypdfium2를 제안해 채택. 로컬에서 한글·한자·희귀 자모·표가 포함된 실제 샘플 PDF로 페이지 렌더링을 직접 확인(육안 검증, 스크린샷). output.py 확장 후 동시성 테스트(`test_finalize_concurrent_same_stem_never_overwrites`, DEC-021)를 포함한 기존 스위트 전체 재통과 확인 | 2026-08-05 |
| DEC-027 | **HWP→DOCX·PDF→DOCX 문자 서식(굵게/기울임/밑줄/글자크기/색상) 반영**(4-phase 계획 Phase 3) — 블록 스키마를 `{"type":"p","text":str}`에서 `{"type":"p","runs":[{"text":str,"bold":bool,"italic":bool,"underline":bool,"size":float,"color":"RRGGBB"}, ...]}`로 확장(구버전 스키마도 서식 없는 단일 run으로 하위 호환). **HWP→DOCX**: `sidecar/hwp/HwpToJson.java`가 문단의 `ParaCharShape`(위치별 글자모양ID 목록)를 `DocInfo`의 `CharShape` 레코드로 역참조해 run별 서식을 뽑는다. **PDF→DOCX**: `app/converters/pdf.py`가 pdfminer의 레이아웃 객체(`LTTextContainer`=문단, `LTChar.fontname`에 "Bold"/"Italic" 포함 여부로 판정)를 문자 단위로 순회. **쓰기**(`app/converters/docx_build.py`): `run.font.bold/italic/underline/size/color.rgb`에 반영(python-docx 기능은 이미 충분, 새 라이브러리 불필요). **PDF는 밑줄을 지원하지 않는다** — PDF는 밑줄을 폰트 속성이 아니라 별도 벡터 선(그림)으로 그리는 경우가 많아 폰트 이름 기반 휴리스틱으로는 원천적으로 판별 불가(모든 PDF run은 항상 `underline:false` — HWP→DOCX만 밑줄 지원). 표 셀 서식·PDF/HWP→HWP 쓰기(DOCX→HWP·PDF→HWP)는 이번 phase 범위 밖(Phase 4) | 사용자 승인(계획 파일 `/Users/jihun/.claude/plans/iridescent-rolling-canyon.md`)으로 Phase 3 착수. **PDF 폰트명 휴리스틱을 로컬에서 실측 검증**: (1) 영문은 폰트가 굵기별 파일로 분리돼 있으면(예: Caladea-Bold) 항상 정확히 감지됨. (2) **한글도 굵기별 폰트 파일이 실제로 분리돼 있으면(예: AppleSDGothicNeo-Bold) 정확히 감지되지만**, 문서에 동아시아 글꼴을 명시하지 않아 렌더러가 임의의 대체 글꼴 하나로 뭉뚱그리면 굵기 정보 자체가 사라져 감지 불가함을 재현 확인(LibreOffice가 한글 미지정 런을 전부 "ArialUnicodeMS" 단일 폰트로 렌더링). (3) 기울임은 한글 글꼴 대부분에 별도 이탤릭 글리프가 없어(CJK 타이포그래피 관행) 렌더러가 애초에 반영 안 하는 경우가 흔함(재현: AppleSDGothicNeo 기울임 요청 시 실제로는 Regular로 렌더링됨) — 우리 휴리스틱의 결함이 아니라 원본 자체에 감지할 서식이 없는 것. 이 한계를 코드 주석·본 로그에 정직하게 문서화. **HWP 쪽 검증**: 실제 공공기관 실사용 문서(`distribution.hwp`)에 자연히 존재하는 굵은 제목(16pt)·문단 내 부분 굵게 강조("(※부가가치세 포함)")로 실사용 검증(합성 픽스처보다 우선). 추가로 기울임·큰 글씨·색상 조합까지 커버하기 위해 테스트 전용 도구 `sidecar/hwp/MakeFormattedHwp.java`(LineSegDebug와 같은 원칙, DEC-018 — 배포용 `engine-build/hwp`가 아닌 CI의 `debug-tools`에 컴파일) 신설. CI(`build.yml`)의 HWP 엔진 스모크에 굵게/기울임/크기/색상 동시 검증 추가. **코드 리뷰로 발견해 수정한 버그 2건**: (1) Form XObject(예: 벡터 그래픽 안에 텍스트 박스) 안의 텍스트가 `LTTextContainer`만 훑는 순회에서 조용히 누락되던 문제 — 직접 재현 확인(`extract_text()`는 잡아내는데 새 레이아웃 순회는 놓침) 후 레이아웃 트리를 재귀 순회하도록 수정. (2) 같은 굵기/기울임 안에서 글자 크기만 바뀌면 run이 안 나뉘고 마지막 크기로 전체가 덮이던 문제 — flush 조건에 크기를 포함하도록 수정, 두 경우 모두 회귀 테스트 추가. **머지 후 재검토(독립 리뷰 에이전트)로 발견한 심각한 회귀 1건 — HWP 쪽 실제 텍스트 손실**: `HwpToJson.java`가 `ParaCharShape`의 위치값을 `charList` 인덱스와 동일하게 취급했는데, 실제로는 `HWPChar.getCharSize()` 가중치 단위(확장/인라인 컨트롤 문자 — 하이퍼링크·섹션/컬럼정의 등 — 는 `charList`에서 1칸만 차지해도 이 가중치로는 8)라 서로 달랐다. `distribution.hwp`(이 기능 검증에 쓰던 바로 그 실사용 문서)로 머지 전/후 전체 텍스트를 직접 diff해 재현: 단독 특수문자 "⑤"가 통째로 사라짐(1글자짜리 run이 hwplib 자체의 `ParaText.getNormalString(start,end)` 버그 — `start==end`면 무조건 빈 문자열 반환 — 에 걸림), 괄호 앞 공백이 2곳에서 소실(가중치 위치를 그대로 `charList` 인덱스로 써서 문단 뒷부분이 밀림). 두 근본 원인 모두 수정: ① `getNormalString()` 대신 `charList`를 직접 순회해 일반 글자만 골라내는 `extractNormalText()`로 교체(라이브러리의 단일 글자 버그 우회), ② `ParaCharShape` 위치를 실제 `charList` 인덱스로 변환하는 `weightedPositionToIndex()` 추가. 앞뒤 공백 제거도 물리적 첫/끝 run만 다듬던 것에서 전체 문자열 기준으로 run 경계를 넘나들며 정확한 양만 제거하도록(`trimEdges()`) 다시 고쳤다(중간 run에 낀 공백을 놓치던 문제). 수정 후 `distribution.hwp` 47개 문단 전체가 머지 전(서식 없던 버전)과 텍스트 완전 일치함을 재확인, 회귀 테스트 추가. **교훈**: 합성 픽스처만으로는 이런 문제를 못 잡는다 — 실제 공공기관 문서로 전체 텍스트 diff까지 해야 드러나는 종류의 결함이었다 | 2026-08-05 |
| DEC-028 | **DOCX→HWP 표 신규 생성**(4-phase 계획 Phase 4, DEC-017 정정) — DOCX의 표가 더 이상 `" \| "` 텍스트로 단순화되지 않고 실제 HWP 표 컨트롤(`ControlTable`/`Row`/`Cell`/`ListHeaderForCell`/`BorderFill`)로 새로 생성된다. 셀 병합·셀 안 문자 서식(굵게 등)은 이번 범위 밖 — 셀 텍스트는 평문 한 문단. `sidecar/hwp/JsonToHwp.java`를 문단·표 블록을 순서대로 처리하도록 재구성(이전엔 모든 블록을 문단 리스트로 먼저 평탄화했음), 표 폭/높이는 열 개수에 비례한 근사치(문서화된 단순화, 기존 줄바꿈 근사치와 같은 원칙) | **DEC-017의 전제 정정**: "hwplib에는 표를 처음부터 만드는 도구·예시가 전혀 없다"는 기록이 틀렸음을 재조사로 확인 — 스파이크 당시와 같은 hwplib 커밋 안에 공식 샘플 `src/test/sample/Inserting_Table.java`(2×2 표 생성 예제)가 이미 있었고 저장소 README에도 색인돼 있었다(이전 조사에서 놓침). **스파이크(계획된 1~2단계) 진행**: `spike/hwplib/SpikeTable.java`에 그 샘플을 이 프로젝트의 `BlankFileMaker` 기반 생성 방식에 맞게 이식 → 로컬에서 2×2 표 생성 후 `HwpToJson`으로 재읽기 왕복 검증(셀 내용 4개 전부 정확히 일치) → 성공을 확인한 뒤에야 프로덕션 코드(`JsonToHwp.java`)에 일반화(임의 N×M, 문단·표가 섞인 문서, 문서 첫 블록이 표인 경우까지 로컬 검증). **알려진 한계(정직하게 문서화)**: (1) 셀 병합 미지원 — DOCX에 병합된 셀이 있으면 병합이 풀려 나타날 수 있음(python-docx가 병합 영역의 각 그리드 위치에 같은 셀 객체를 돌려줘 내용이 중복 표시될 수 있음, 후속 과제). (2) **hwplib 자체 왕복 검증(이번에 한 것)은 실제 한글/한워드 뷰어의 렌더링을 보장하지 않는다** — DEC-018 때와 완전히 같은 근본적 제약(Mac 개발 환경에 뷰어가 없음). 표 위치·테두리·셀 크기의 정확한 수치(`CtrlHeaderGso`의 offset/width/height, `BorderFill`)는 열 개수 비례 근사치일 뿐 실제 hwplib 표본에서 역산한 값이 아니라 LineSeg 근사치보다도 검증 수준이 낮음 — **Windows 실사용자가 실제 한글 뷰어로 열어 확인하기 전까지는 "완료"로 표시하지 않는다**(다음 Windows 테스트 라운드에 포함 필요, `testing/WINDOWS_TEST_GUIDE.md`). 유닛 테스트(`tests/test_hwp_table_generation.py`)와 CI(`build.yml`)의 HWP 엔진 스모크에 표 구조(행렬 수·셀 내용) 왕복 검증 추가 | 2026-08-05 |
| DEC-029 | **macOS 배포판 릴리스**(REQ-NF-001 정정, DEC-014 보류 결정을 뒤집음) — **Apple Silicon(arm64) 전용, v1**. GitHub Actions `macos-latest` 러너가 arm64 네이티브라 이 아키텍처로만 빌드(Intel Mac/x86_64는 PyInstaller universal2가 모든 의존성의 universal2 wheel을 요구해 범위 밖, 필요 확인되면 후속 라운드). **서명 없이 배포(무료)** — Apple Developer Program 미가입, Gatekeeper가 막으므로 README에 "control+클릭 → 열기" 안내 추가. **영상→MP4는 v1에서 미지원** — Windows에 번들한 FFmpeg(BtbN/FFmpeg-Builds, DEC-024)는 macOS 빌드를 아예 제공하지 않고(저장소 README가 "Darwin은 build script를 보라"고 명시, 사전 빌드 바이너리 없음), 대안 `mifi/ffmpeg-builds`도 릴리스 자산에 macOS가 없음을 `gh api`로 직접 확인 — 검증된 사전 빌드 LGPL macOS 바이너리가 없는 채로 소스 빌드를 자체 진행하는 건 별도 라운드 규모라 v1 범위 밖으로 좁힘. `app/converters/__init__.py`가 `video.find_ffmpeg() is None`이면 영상 확장자를 `TARGETS`에서 아예 빼(가능한 것만 노출 — C-03) "재설치하세요" 같은 엉뚱한 오류 대신 자연스럽게 미지원으로 처리(단위 테스트 `tests/test_video.py::TestVideoTargetsPlatformGating`). **배포 포맷은 `.dmg`**(macOS 내장 `hdiutil`, 추가 유료 도구 불요) — Windows Inno Setup 인스톨러에 대응하되 "앱을 Applications로 드래그" 수준(REQ-NF-006). CI `build-macos` 잡은 `build-windows`와 병렬 실행(같은 hwplib 리비전·LibreOffice 26.2.5·Noto Sans KR 재사용), FFmpeg 단계만 생략. 두 잡이 각자 `gh release create`를 부르면 같은 태그에 경쟁 상태로 하나가 실패할 수 있어, 발행은 두 잡을 모두 기다리는 별도 `publish-release` 잡으로 분리해 Windows exe와 macOS dmg를 같은 릴리스에 함께 첨부 | LibreOffice macOS 배포 구조(`.app` 내부 경로)는 실제 다운로드해 직접 확인: soffice 바이너리 `LibreOffice.app/Contents/MacOS/soffice`, 폰트 디렉터리 `LibreOffice.app/Contents/Resources/fonts/truetype`(기본 상태 한글/CJK 0개 — Windows와 같은 갭, Noto Sans KR 동일 적용), 라이선스 `LibreOffice.app/Contents/Resources/LICENSE*`·`NOTICE`. SHA256(mac/aarch64, 26.2.5) 공식 서버에서 직접 확인. `find_soffice()`(`app/converters/office.py`)의 번들 경로 탐색이 Windows 전용(`libreoffice/program/soffice.exe`)만 확인하고 macOS `.app` 구조를 놓치는 버그를 자체 점검 중 발견 — macOS 경로 분기 추가 + 회귀 테스트(`tests/test_office.py`) 작성. `app/bundle.py`의 `engine_dir()`(exe 옆 `engine/`)가 macOS 앱 번들에서는 `Contents/MacOS/engine`을 의미함(`Contents/Resources`가 아님, `sys.executable`이 `Contents/MacOS/실행파일`을 가리키므로) — 로컬에서 가짜 `.app` 레이아웃을 만들어 `scripts/smoke_pdf_pipeline.py --frozen-exe`로 자동 탐색 전체 경로를 실제로 재현·검증함. **잔여 리스크**: 완전 클린 macOS(Gatekeeper 상태 포함) 실기기 검증 미실시 — CI 러너는 이미 개발자 도구가 깔린 환경이라 일반 사용자 경험과 다를 수 있음(Windows의 "VC++ 런타임 없는 클린 환경 미검증"과 같은 성격) | 2026-08-05 |
| DEC-030 | **PDF→PPTX 신규 지원**(REQ-F-017) — 페이지 전체를 이미지 한 장으로 뭉개는 방식(스파이크로 먼저 검증, 기술적으로는 동작함을 확인) 대신, pdfminer로 줄 단위 위치(bbox)를 추출해 각 줄을 원본과 같은 위치·크기의 **개별 텍스트 상자**로 재구성 — 진짜 편집 가능한 텍스트로 남는다(파일 크기도 이미지 방식 대비 훨씬 작음, 스파이크에서 32KB vs 456KB로 직접 확인). 굵게 판정은 `pdf_to_docx`(DEC-027)와 같은 폰트 이름 휴리스틱(`_container_to_runs`)을 그대로 재사용. **알려진 한계(정직하게 문서화)**: (1) 표 테두리·이미지·도형 등 텍스트가 아닌 요소는 옮겨지지 않는다 — PDF에서 이들은 별도의 벡터 그림이라 텍스트 레이어 추출 범위 밖(변환 전 UI 고지 `note.pdf_to_pptx`로 안내). (2) 재구성에 항상 Noto Sans KR을 지정하므로(DEC-015와 같은 이유) 원본 폰트와 글자 폭이 달라 드물게 줄바꿈이 살짝 밀릴 수 있음(스파이크에서 실제로 재현·확인). (3) 기울임은 한글 글꼴 대부분에 별도 이탤릭 글리프가 없어 감지되지 않는 경우가 흔함(`pdf_to_docx`와 동일 제약, DEC-027 문서화 재확인) | 스파이크로 두 방식(전체 이미지 vs 줄 단위 재구성)을 직접 만들어 LibreOffice로 왕복 렌더링한 뒤 시각 비교 — 줄 단위 재구성이 원본과 거의 동일한 레이아웃으로 나옴을 확인(표는 텍스트 위치는 맞지만 테두리 없음, 예상대로). python-pptx는 지금까지 스모크 테스트 픽스처 생성용으로만 있었는데 이제 실제 런타임 의존성이 되어 requirements.txt로 승격 — PyInstaller가 `pptx/templates/default.pptx`(코드가 아닌 데이터 파일, `Presentation()` 생성에 필수)를 자동으로 놓칠까 우려해 로컬에서 실제로 얼려(freeze) `Presentation()`을 호출해봤으나, `pyinstaller-hooks-contrib`의 공식 `hook-pptx.py`가 이미 처리하고 있음을 확인(불필요한 `--collect-data` 플래그를 추가했다가 검증 후 되돌림) — 다만 PIL/pypdfium2와 같은 이유로 CI에 번들 존재 여부 회귀 검증은 추가함(DEC-025와 같은 방어적 관행) | 2026-08-06 |
| DEC-031 | **인스톨러 아키텍처 지시자 추가**(외부 QA #48 수정) — `packaging/installer.iss`에 `ArchitecturesInstallIn64BitMode=x64compatible`·`ArchitecturesAllowed=x64compatible` 추가. 이 지시자가 없으면 Inno Setup이 32비트 설치 모드로 동작해, "모든 사용자용으로 설치"를 선택했을 때 `{autopf}`가 `{commonpf32}`(`Program Files (x86)`)로 풀렸다 — 번들이 전량 64비트인데도 32비트 전용 디렉터리·레지스트리 뷰(`WOW6432Node`)에 설치되는 문제(기능 자체는 정상 동작하지만 위치·레지스트리 뷰가 아키텍처와 불일치). CI는 무인 설치(기본값=사용자 전용)만 검증해 이 분기를 지금까지 한 번도 타지 않았음 | 외부 QA가 실제 "모든 사용자용" 설치를 수행해 `C:\Program Files (x86)`에 1,906MB·20,859파일이 깔리고 `HKLM\...\WOW6432Node\...\Uninstall`에 등록됨을 실측, 번들 바이너리(`FileConverter.exe`·`ffmpeg.exe`·`java.exe`·`soffice.exe`) PE 헤더를 직접 읽어 전량 x64임을 확인 후 이슈 제출. CI에는 컴파일된 `installer.iss` 소스에 두 지시자가 남아 있는지 확인하는 정적 검증만 추가함 — **UAC 승격이 필요한 "모든 사용자용" 설치를 무인 CI로 실제로 완주시켜 검증하는 것은 승격 대화상자가 무인 환경에서 멈출 위험이 있어 보류**(잔여 리스크로 남김, 다음 실사용 Windows 테스트 라운드에서 수동 확인 필요) | 2026-08-06 |
| DEC-032 | **HWP→DOCX·PDF의 표 외 컨트롤(머리말·글상자) 텍스트 무고지 유실 수정**(외부 QA #43, v0.2부터 존재하던 회귀 — 신규 아님) — `sidecar/hwp/HwpToJson.java`가 문단의 컨트롤 목록에서 `ControlType.Table`만 처리하고 나머지(머리말·꼬리말·글상자·각주·미주)는 전부 건너뛰어, 공문·서식의 문서 제목·결재란·서명란이 조용히 사라지고 있었다(변환은 "성공"으로 표시됨). `emitParagraph`/`emitTable`/`emitGso`로 재귀 구조로 재작성 — 머리말·꼬리말·각주·미주는 `Control`이 바로 문단 리스트를 가지므로 그 문단들을 다시 `emitParagraph`로 재귀 처리(중첩된 표까지 자동으로 잡힘), 글상자(도형)는 텍스트를 담을 수 있는 하위 타입(사각형·타원·다각형·곡선·호)의 `TextBox`에서, 묶음 개체(`ControlContainer`)는 차일드 컨트롤 목록을 재귀로 순회(그룹 중첩도 지원). 수식·양식 개체 등은 이번 범위 밖(정직하게 문서화). HWP→PDF도 내부적으로 DOCX를 경유하므로 동일하게 해결됨. HWP→TXT는 애초에 `TextExtractor`의 `InsertControlTextBetweenParagraphText` 옵션으로 컨트롤 텍스트를 이미 포함하고 있어 영향 없었음(그래서 TXT와 DOCX 결과가 서로 달랐던 것) | 실사용 공공기관 문서 5건(`research/hwp-coverage/fetch_samples.sh`)으로 TXT↔DOCX 글자 다중집합 비교 재현 — 수정 전 3건에서 실제 유실 확인(mois-hwpplan.hwp 69자 "검토보고" 등 결재 헤더, unikorea-contract.hwp 15자 서명란). 1차 수정(표 아닌 컨트롤의 "직접" 텍스트만 처리)으로는 2건이 남았다 — 재조사로 두 가지 중첩 패턴을 추가 발견: mois-hwpplan.hwp는 머리말 **안에 표가 중첩**(결재란이 이 모양), unikorea-contract.hwp는 **묶음 개체 안에 글상자 2개가 중첩**(서명란) — 재귀 구조로 재작성해 5건 전부 TXT↔DOCX 완전 일치(유실 0, 중복 0)로 확인. 기존 회귀 기준선(`distribution.hwp`, PR #37에서 검증한 문서)도 여전히 완전 일치함을 재확인해 이번 수정이 이전 수정을 깨지 않았음을 확인. 회귀 방지용 합성 픽스처 `sidecar/hwp/MakeNestedControlHwp.java`(머리말 안 표 + 묶음 개체 안 글상자 2개를 hwplib API로 직접 생성 — 제3자 문서를 저장소에 넣지 않기 위해 합성) 신설, 수정 전 코드로 돌리면 이 픽스처가 `{"blocks":[]}`(완전 공백)를 내는 것으로 픽스처 자체의 민감도까지 확인. CI(`build.yml`)의 HWP 엔진 스모크(Windows+macOS 둘 다)에 이 픽스처 기반 검증 추가. 상세 조사 기록: `research/hwp-coverage/RESULT.md` 2026-08-06 추가 섹션 | 2026-08-06 |
| DEC-033 | **영상 구성 요소 미발견 오류 문구에 백신 오탐 가능성 추가**(외부 QA #42) — `err.video_missing` 문구를 "앱을 다시 설치해 주세요"에서 "백신 프로그램이 위협으로 오인해 격리했을 수 있어요 — 백신의 격리 목록을 먼저 확인해 보시고, 그래도 안 되면 재설치"로 정정. 번들 FFmpeg(BtbN LGPL 커뮤니티 빌드, DEC-024)만 무서명이라(java.exe·soffice.exe·soffice.bin은 유효 서명 확인됨) Defender ML 휴리스틱(`Wacapew.A!ml`)에 격리된 사례가 실사용 중 1건 관측됨 — 격리가 활성인 동안에는 재설치해도 실행하면 다시 격리되므로 기존 문구는 사용자를 틀린 조치로 이끌었음(서명 갱신으로 오탐이 해소된 뒤에는 재설치도 불필요했음). 근본 해결(코드 서명 인증서)은 비용 의사결정이 필요해 별도 — 이번엔 원인을 정확히 짚는 문구 정정만 진행 | 외부 QA가 `Get-MpThreatDetection`으로 격리 이력 확인, `Get-AuthenticodeSignature`로 번들 엔진 4종 중 FFmpeg만 무서명임을 실측, 서명 갱신 시각과 탐지 해소 시각의 선후 관계로 인과를 추정(Microsoft 측 변경 내역으로 직접 확인한 것은 아님 — 단일 기기 관측의 잔여 불확실성으로 문서화됨). `find_ffmpeg()`가 None일 때 macOS처럼 TARGETS에서 영상 확장자를 미리 빼는 방어는 이미 앱 시작 시점 기준으로는 되어 있음(DEC-029) — 다만 설치 직후 정상이던 ffmpeg가 **앱 실행 도중** 격리되는 이번 시나리오는 TARGETS가 시작 시점에 한 번만 계산되는 구조상 잡히지 않아, 문구 정정만으로 대응(TARGETS를 변환 시점마다 재계산하는 구조 변경은 이번 범위 밖 — 다른 엔진들도 동일한 시작-시점 캐시 방식이라 영상만 다르게 만들면 일관성이 깨짐) | 2026-08-06 |
| DEC-034 | **문서·UI 문구 정확성 일괄 정정**(외부 QA #44·#45·#46·#47, 전부 코드 결함이 아니라 문서/문구가 실제 동작과 어긋난 경우) — (1) #44: README·v0.3.8 변경이력의 "한글 이탤릭만 감지 안 됨" 서술을 "한글은 굵게·기울임 둘 다 거의 항상 감지 안 됨(문서에 동아시아 글꼴이 명시된 드문 경우만 동작, 라틴 문자는 정상)"으로 정정 — DEC-027 결정 로그 자체("근거" 칸)는 이미 정확했고 README 요약만 과소평가돼 있었음. (2) #45: README·`testing/WINDOWS_TEST_GUIDE.md`의 SmartScreen 서술을 "파란 경고창이 뜰 수 있다"에서 "빨간 차단 화면이 뜨며 첫 화면엔 실행 버튼이 없다(추가 정보를 먼저 눌러야 함)"으로 정정. (3) #46: README 다운로드 안내·테스트 가이드에 "설치 모드 선택" 화면의 존재를 명시하고 "관리자 권한 불요"가 기본값 선택 시에만 참임을 분명히 함. (4) #47: 설정 메뉴 `settings.update_check`를 "업데이트 확인"에서 "새 버전 알림"으로, 안내 문구 `update.available`에 "다운로드 페이지 열기"를 추가해 앱 내 자동 업데이트로 오인되지 않게 함(README 자체는 이미 정확했음 — UI 문구만 기대치를 오도) | 넷 다 외부 QA가 실제 조작(설치 화면 스크린샷, 코드 대조, 클릭 후 브라우저 이동 확인)으로 문서와 실제 동작의 구체적 불일치를 짚어 제출. #44는 DEC-027 재조사로 "가끔 놓침"이 아니라 "거의 항상 실패"임을 재확인(라틴 문자만 정상 동작). #47은 앱 내 자동 업데이트 코드 자체가 없음을 `update_check.py`·`main_window.py` 전수 검색으로 직접 확인 후 제출된 리포트 — 코드 동작은 이미 REQ-NF-002(네트워크 요청 최소화) 원칙과 일치했고 문구만 바꿈 | 2026-08-06 |
| DEC-039 | **PDF→HWP 페이지 구분 유지**(외부 QA 피드백, 2026-08-07) — PDF 여러 페이지의 텍스트가 HWP 안에서 페이지 구분 없이 한 페이지처럼 이어 붙어 레이아웃이 깨지던 버그(텍스트 자체는 정상 — 순수 레이아웃 문제). 원인: `pdf_to_hwp`가 `pdfminer.extract_text()`로 문서 전체를 한 문자열로 뽑은 뒤 빈 줄 기준으로만 문단을 나눴는데, `extract_text()`가 페이지 경계 자체를 표시하지 않았다. `app/converters/pdf.py`에 `_extract_pdf_blocks_by_page()`를 신설해 `extract_pages()`로 페이지 단위로 직접 순회하고, 각 페이지 첫 문단에 `pageBreakBefore:true`를 표시하는 블록 스키마 확장(`{"type":"p","text":str,"pageBreakBefore":bool}`)을 추가했다. `sidecar/hwp/JsonToHwp.java`가 이 표시가 있는 문단에만 기본 ParaShape(id=3)을 복제해 "문단 앞에서 항상 쪽 나눔"(`ParaShapeProperty1` 19bit, `setSplitPageBeforePara`)을 켠 새 ParaShape을 걸어(한 번만 만들어 캐시) 실제 HWP 쪽 나눔으로 반영한다. 이제 안 쓰게 된 `docx_build.text_to_blocks()`(pdf_to_hwp의 유일한 호출자였음)는 삭제. DOCX→HWP·표 블록에도 같은 `pageBreakBefore`를 받을 수 있게 일반화했지만(JsonToHwp 쪽), 실제로 이 값을 채우는 건 이번엔 `pdf_to_hwp`뿐 — DOCX→HWP는 범위 밖(DOCX 자체엔 페이지 경계 개념이 약해 이 버그가 없었음) | **스파이크 먼저**: hwplib 소스(`ParaShapeProperty1.java`)에서 "문단 앞에서 항상 쪽 나눔 여부"(19bit) 게터/세터를 직접 찾아 `spike/hwplib/SpikePageBreak.java`로 write+read 왕복 시 이 비트가 정확히 보존되는지 먼저 검증한 뒤에야 프로덕션 코드(JsonToHwp.java)에 반영. HwpToText/HwpToJson 둘 다 이 ParaShape 속성을 안 다뤄(문서 텍스트·표 구조만 봄) 텍스트 왕복만으로는 회귀를 못 잡아 테스트 전용 디버그 도구 `sidecar/hwp/PageBreakDebug.java`(LineSegDebug·MakeFormattedHwp와 같은 원칙, DEC-018·DEC-027)를 신설해 문단별 실제 ParaShape 속성을 직접 확인. QA가 보고한 "HWP→DOCX/PDF 표 병합 안 됨"(같은 피드백 묶음의 다른 항목)은 조사 결과 이미 리뷰 대기 중인 DEC-035(PR #55, 별도 워크트리에서 직접 빌드·실행해 병합 셀 HWP→DOCX 변환이 실제로 `gridSpan`을 만드는 것까지 확인)로 해소되는 문제라 별도 구현 없이 그 PR 머지로 넘김. 로컬 전체 테스트 통과(순수 파이썬 `_extract_pdf_blocks_by_page` 단위 테스트는 JDK 불요, hwplib 왕복 통합 테스트는 로컬 빌드 있을 때만 실행) + CI(`build.yml`) Windows·macOS HWP 엔진 스모크에 페이지 구분 왕복 체크 추가(로컬 hwplib 빌드로 실제 실행해 출력 형식이 정규식/문자열과 일치함을 직접 확인 후 커밋, DEC-038과 같은 검증 방식) | 2026-08-07 |

## 리스크
| 리스크 | 심각도 | 완화책 |
|--------|:------:|--------|
| ~~HWP TXT/DOCX 오프라인 변환 품질~~ → **해소(2026-07-29)**: hwplib 스파이크 통과(공식 샘플) + 실사용 문서 검증(OQ-006, 공공기관 5건+hwplib 배포용 1건 총 6/6) 모두 통과. TXT/DOCX 범위에서 Must 유지 확정 (DEC-007·DEC-011) | ~~높음~~ → **낮음 (TXT/DOCX 한정)** | 1단계 기대치는 "텍스트+표 내용+기본 구조" 수준으로 고지(DEC-010). 사진·차트 중심 문서, 진짜 암호화 문서는 표본 밖 — 필요 시 추가 라운드 |
| **HWP→PDF 실사용 커버리지** — CI에서는 hwplib 샘플로 파이프라인 동작 확인(DEC-012), 공공기관 실사용 5건 기준 PDF 검증은 아직 별도 라운드로 남음 | 낮음 (기존 중간에서 하향) | v0.3b로 파이프라인 자체(HWP→DOCX→LibreOffice)는 CI 게이트로 매번 검증됨. OQ-006의 실사용 5건으로 PDF까지 재검증하는 것은 후속 research 라운드로 진행 |
| ~~LibreOffice 번들 라이선스(MPL)·재배포·패키징 복잡도~~ → **해소(2026-07-29)**: 버전 고정(26.2.5)·SHA256 검증·MPL 2.0 고지 포함 완료 (DEC-012) | ~~중간~~ → **낮음** | 잔여: 완전 클린 Windows(VC++ 런타임 없음)에서의 동작은 CI(러너에 이미 VC++ 런타임 존재)로는 완전히 검증되지 않음 — 실기기 검증 권장 |
| **완전 클린 Windows에서 LibreOffice 실행 미검증** (신규) | 중간 | GitHub Actions windows-latest 러너에는 VC++ 런타임이 이미 있어 CI 통과가 곧 "모든 사용자 PC에서 동작"을 보장하지 않음. 배포 전 VC++ 런타임이 없는 실제/가상 클린 Windows에서 1회 이상 수동 검증 권장 |
| PDF→DOCX 정확도에 대한 일반 사용자 기대와 기술 한계의 간극 | 중간 | 변환 전 안내 문구(OQ-003), 결과 요약에 한계 고지 |
| ~~PDF/HWP→DOCX 글자 깨짐(우리 앱 내부 렌더링)~~ → **해소(2026-07-30)**: Noto Sans KR 명시 지정으로 해결, 스모크로 폰트 임베딩까지 검증 (DEC-015). **단, 아래 항목대로 이 해소는 우리 앱 자신의 렌더링 경로에 한정** | ~~높음~~ → **낮음(우리 앱 경로 한정)** | — |
| **PDF/HWP→DOCX 결과물을 사용자의 Word/한글(HWP)에서 직접 열 때의 렌더링 — 미검증** (신규, 재리뷰에서 지적) | 중간 | `w:eastAsia="Noto Sans KR"`을 지정하지만 이 폰트는 우리 앱 안에만 번들되어 사용자의 별도 Word/한글에는 보이지 않는다 — 그 프로그램 자체의 글꼴 대체 로직에 의존. 대체로 "맑은 고딕"(Windows 기본, 상시 설치) 지정을 시도했으나 폰트 없는 환경에서 대체 로직이 오히려 더 나쁜 결과를 내는 회귀가 확인되어 채택 보류(DEC-015). 실제 Word/한글에서 열어보는 것은 CI로 검증 불가 — 다음 Windows 실사용자 테스트 항목에 포함(`testing/WINDOWS_TEST_GUIDE.md`) |
| **사용자가 직접 제공한 DOCX/PPTX(글꼴 미지정)의 DOCX→PDF 렌더링 — 호스트 글꼴 의존 잔존** | 중간 | 사용자가 가져온 기존 DOCX/PPTX는 그 파일이 지정한 글꼴(또는 무지정)을 그대로 따른다. LibreOffice의 폰트 대체 로직이 항상 번들 Noto Sans KR로 귀결되는 것은 아님을 로컬 검증 중 직접 확인. 대부분의 실제 한글 Word 문서는 맑은 고딕(Windows 기본 글꼴, 상시 설치됨)을 사용해 영향이 제한적일 것으로 추정되나 미검증. 필요 시 LibreOffice 폰트 대체 테이블(관공서 서식에 흔한 함초롬바탕 등 포함) 구성을 후속 작업으로 검토 |
| mac 개발 ↔ Windows 배포 간 폰트·경로·패키징 차이 | 중간 | 초기부터 Windows VM/실기기에서 주기적 검증 |
| ~~DOCX→HWP 생성 문서를 실제 한글/한워드 등 외부 뷰어에서 열 때의 렌더링 — 미검증~~ → **실사용 확인(2026-08-04)**: Windows 테스터가 여러 파일을 변환해 직접 열어봄. 텍스트·간단한 표 위주 문서는 완전히 깔끔하게 변환되고, 복잡한 서식이 들어간 문서는 서식은 깨지지만 텍스트는 전부 보존됨(DEC-018) | ~~중간~~ → **낮음(기대치와 일치)** | 결과가 정확히 OQ-003/DEC-010이 애초에 고지한 기대치("텍스트+표 내용은 유지되고 레이아웃은 단순화될 수 있음")와 일치 — 새로운 결함이 아니라 설계대로 동작. 복잡한 서식까지 보존하려면 근본적으로 다른(더 무거운) 렌더링 접근이 필요해 별도 과제로 분리 |
| XLSX→CSV 다중 시트 — 여전히 첫 시트만 변환(고지만 추가, DEC-019) | 낮음 | 여러 시트를 각각 파일로 출력하는 방안은 데이터 모델 변경이 필요해 별도 과제로 보류. 최소한 조용한 데이터 유실은 막음(변환 전 고지) |
| **영상→MP4는 H.264/HEVC 소스만 지원**(DEC-024, v1 의도적 범위 제한) — **실사용 확인(2026-08-04)**: Windows 테스터가 영상 변환이 정상 동작함을 확인 | 낮음 | 오래된 코덱(Xvid/DivX 등) 영상은 명확한 오류 메시지로 거부됨(조용한 실패 아님). 실사용에서 이 코덱들에 대한 지지가 확인되면, 영상 인코더 선택(OpenH264 화질 문제 확인됨/GPL x264/Windows 전용 h264_mf 미검증) 문제를 다시 풀어야 함 — 별도 라운드로 분리 |
| **인스톨러 exe가 코드 서명되지 않아 Windows SmartScreen 경고가 뜸** (신규, 실사용 테스터 리포트 2026-08-04) | 낮음(불편하지만 차단 아님) | Microsoft의 "인식할 수 없는 앱" 경고는 정식 코드 서명 인증서(OV/EV, 연 단위 비용 발생) 없이는 근본적으로 없앨 수 없다 — 사이드 프로젝트 특성상 현재는 감수. "추가 정보 → 실행" 경로로 계속 진행 가능함을 README/테스트 가이드에 고지하는 정도가 현실적인 완화책. 서명 인증서 구매는 별도 비용 의사결정이 필요해 사용자 확인 후 진행 |
| **DOCX→HWP 신규 생성 표(DEC-028)를 실제 한글/한워드 뷰어에서 열 때의 렌더링 — 미검증** (신규) | 중간 | hwplib 자체 왕복 읽기(HwpToJson)로는 셀 내용·행렬 수까지만 확인 가능 — 표 위치·테두리·셀 크기(`CtrlHeaderGso`/`BorderFill`)는 열 개수 비례 근사치일 뿐 실제 뷰어가 어떻게 그릴지는 다른 문제(DEC-018과 동일한 근본적 제약, Mac 개발 환경에 한글 뷰어가 없음). 위 "간단한 표 위주 문서는 깔끔하게 변환"이라는 2026-08-04 실사용 확인은 **표를 텍스트로 단순화하던 이전 버전** 기준이라 이 신규 표 생성 기능에는 적용 안 됨 — 다음 Windows 테스트 라운드에서 별도로 확인 필요(`testing/WINDOWS_TEST_GUIDE.md`에 추가). 셀 병합 미지원도 같이 안내 |
